# Phase 22 Agent A5 — Comprehensive audit

Date: 2026-05-07
Owner: P22-A5
Scope: cross-cutting metric snapshot for the in-flight Phase 22 effort
(107 sub-3-paragraph standard cells → 0 + 18 `depth_inversion` cells →
0 cleanup). Owned scope is read-only measurement under
`lib/spring-ts/artifacts/phase22-agent-a5/`. No data, src, tools, or
package.json mutation.

## Headline

Phase 22 is the multi-agent fix-through on P21-A4's recommendation
(Path A + B-extended) plus P21-A2's `depth_inversion` 18 hits cleanup.
Phase 21 closed five tracks (PR #537-#541) — A1 verb-form 연결 →
이어지는, A2 `depth_inversion` Levenshtein refresh, A3
`ci:hook-concentration --max-hooks=20`, A4 sub-3 cells investigation
(audit-only), A5 comprehensive audit. Phase 22's five-agent rotation
attacks the two carry-over fix targets:

- A1: src lift `MINOR_LIMITED_CATEGORIES` early-return + brief tier
  fragments where needed (lift-safe authoring under 9 LOC)
- A2: 14 minor-ageband fallback fragments authoring (career +
  health_stress + health + overall, all 0-9 / 10-19 ageBand)
- A3: 5 wealth-gap fragments authoring (Path A residue closure)
- A4: 18 depth_inversion cell rephrase (1 shared template + 4
  single-fixture overlaps)
- A5: this audit + measurement

At measurement time A1, A2, A3, A4 are landing in parallel branches
(working tree contention observed; per-gate identity-tag defensive
capture used). This audit captures the **clean main HEAD `9e7aafd`
pre-fix snapshot**. Each metric below has a target value, an owning
agent, and a verifier path.

Three findings dominate this audit:

1. **All 14 production CI gates + `ci:hook-concentration` PASS at clean
   main `9e7aafd`** — the `ci:hook-concentration` gate landed in
   Phase 21 (PR #539) and is now part of `tools/acceptance-manifest.json`
   `accepted[]` (so `ci:acceptance-completeness` reports `ciScripts=15,
   accepted=14, exempt=1`). Pre-fix snapshot has zero failing gates —
   second consecutive all-PASS pre-fix capture (P21-A5 was first).

2. **Sub-3 standard cells = 107 (target 0).** Distribution unchanged
   from P21-A4: 75 src-locked via `MINOR_LIMITED_CATEGORIES` early-return
   + 32 data-overridable fallback. Phase 22 closes via three coordinated
   tracks: A1 lifts the early-return (52 cells close instantly via
   existing minor-gated fragments + 23 require new wealth fragments),
   A2 authors 14 fragments for the 32 fallback cells, A3 authors 5
   wealth-gap fragments for the lift residue. Combined target: 107 → 0.

3. **`depth_inversion` hits = 18 (target 0 or ≤ 2).** P21-A2 refreshed
   the detector to Levenshtein similarity ≥ 0.75 on particle-stripped
   normalized forms. The pre-fix snapshot reproduces P21-A2's audit:
   14 hits in `thisMonth/movement` shared template (1 fragment fix
   resolves all 14) + 4 single-fixture overlaps (`thisYear/career`,
   `thisMonth/expression_children`, `today/overall`, `today/academic`).
   P22-A4 ships the rephrase. Combined target: 18 → 0.

## Agent inventory at audit time

| Agent | Branch | HEAD (audit time) | Status |
|-------|--------|-------------------|--------|
| P22-A1 | `phase22-agent-a1` | `9e7aafd` | scope: src lift `MINOR_LIMITED_CATEGORIES` early-return (≤ 9 LOC) + minor-gated wealth brief fragments (only if pre-measurement shows brief-tier regress); committed work surface includes `artifacts/phase22-agent-a1/measure_brief_tier.mjs` (uncommitted at audit time) |
| P22-A2 | `phase22-agent-a2` | `9e7aafd` | scope: 14 fallback fragments authoring (career/health_stress/health/overall × period × ageBand) — uncommitted at audit time, working-tree visible only |
| P22-A3 | `phase22-agent-a3` | `9e7aafd` | scope: 5 wealth-gap fragments authoring (Path A residue) — uncommitted at audit time, working-tree visible only |
| P22-A4 | `phase22-agent-a4` | `9e7aafd` | scope: 18 depth_inversion cells rephrase — uncommitted at audit time |
| P22-A5 | `phase22-agent-a5` (this branch) | (this audit) | this document |

**Worktree contention** (carry-over from P19/P20/P21-A5): the working
tree initially flipped between branches mid-session (HEAD observed at
`phase22-agent-a2`, then `phase22-agent-a3`, then `phase22-agent-a4`
after `git checkout phase22-agent-a5`). The four parallel-fix agents
push their data writes into the shared checkout. P22-A5 mitigated via
defensive per-gate `[branch=… head=…]` identity tagging on each gate
output line — every gate run captures the actual checkout state at
that gate's invocation, so any drift is visible in the artifact. Data
writes from other agents were stashed pre-gate via
`git stash push -- 'lib/spring-ts/data/'` to avoid contaminating the
clean-main pre-fix snapshot. This is the **fourth consecutive A5 audit
observing this contention**; the workflow-level fix is still
OAuth-blocked.

## Measurement methodology

### Tooling produced this phase

| File | Purpose |
|------|---------|
| `measure_p22.mjs` | Successor to `measure_p21.mjs` — adds (a) brief tier PLACEHOLDER count via headline equality with `'준비 중인 흐름이에요.'`, (b) `depth_inversion` passthrough by invoking `audit-phase12.mjs` and reading `phase12-audit-issues.json`, (c) sub-3 sample row enumeration |
| `pre-fix-snapshot-2026-05-07.json` | Output of `measure_p22.mjs` against clean main `9e7aafd` |
| `phase22-acceptance-gates.txt` | Snapshot of all 14 production CI gates + `ci:hook-concentration` + `test:namespring-compat`, each with per-gate identity tags |
| `audit-2026-05-07-phase22.md` | This document |

The measurement script extends `measure_p21.mjs` with three P22 additions:

1. **Brief tier PLACEHOLDER count.** Detects cells where
   `cell.brief.headline === '준비 중인 흐름이에요.'` — the
   `PLACEHOLDER_BRIEF` literal at
   `src/report/tiered/build-tiered-matrix.ts:55`. P22-A1's lift of
   `MINOR_LIMITED_CATEGORIES` opens the brief tier path for minor
   fixtures × wealth/romance/study_document. If no minor-gated brief
   fragment exists, the brief tier regresses to `PLACEHOLDER_BRIEF`.
   Pre-fix baseline: 0. Target: 0 (hold throughout Phase 22).

2. **`depth_inversion` hits passthrough.** Invokes
   `artifacts/phase12-agent-a5/audit-phase12.mjs` (P21-A2 refreshed
   detector — Levenshtein similarity ≥ 0.75 on particle-stripped
   normalized forms) and parses `phase12-audit-issues.json` to count
   `detectorHits.depth_inversion`. Pre-fix baseline: 18. Target: 0
   (or ≤ 2) after P22-A4 rephrase.

3. **Sub-3 standard cells sample row enumeration.** Adds
   `paragraphRollups.sub3StandardCellsSample` (first 10 rows) so
   downstream operators can quickly verify the 107 cells without
   re-loading the full sample-outputs corpus.

The verb-form 연결 scan (`yeongyeolVerb`), 8-particle conservative
scan (`yeongyeolCompound`), GYEOL_SUBS-vulnerable subset, doubled
`결이` cluster, sub-3-paragraph standard cell count, post-processor
leak monitor, per-fixture hook histogram, and hook concentration
distribution stats all carry through unchanged from P21-A5.

### Acceptance gates run (worktree at `9e7aafd`, per-gate identity tag)

| Gate | Status | Note |
|------|--------|------|
| `ci:no-ai-policy` | PASS | fixtureFiles=429, sourceTierRecords=7097 |
| `ci:narrative-voice` | PASS | 4079 fragments, 0/0/0 violations |
| `ci:narrative-truncated-endings` | PASS | filesScanned=344, 0 violations |
| `ci:narrative-tag-label-alignment` | PASS | glossaryTags=208, 0 missing/mismatch |
| `ci:narrative-orphan-tags` | PASS | filesScanned=344, 0 violations |
| `ci:acceptance-completeness` | PASS | 14 accepted + 1 exempt = 15 ci scripts, 0 violations |
| `ci:post-processor-grammar` | PASS | filesScanned=32, violations=0 (max=0 P20-A3 ratchet locked) |
| `ci:samples-stale` | PASS | sampleFiles=37, violations=0 |
| `ci:narrative-density` | PASS | 8 pairs covered, 0 thin/deficit |
| `ci:narrative-tuple-density` | PASS | 3 tuples covered, 0 missing/thin/deficit |
| `ci:narrative-daymaster-tuple-density` | PASS | 1 tuple covered, 0 missing/thin/deficit |
| `ci:narrative-cell-axis` | PASS | 165 cells, 0 missing audience/sajuCore/seasonContext |
| `ci:hook-coverage` | PASS | 34/34 authored, 0 dormant by fragmentId, 5-row 2-emit fragility tail visible |
| `ci:hook-concentration` | PASS | max=15 vs threshold=20 (P21-A3 ratchet, 5-hook headroom) |
| `test:namespring-compat` | PASS | 202 / 0 |

All 14 production CI gates + `ci:hook-concentration` PASS at clean
main `9e7aafd`. Second consecutive all-PASS pre-fix snapshot.

## Phase 22 acceptance gate definition

Each metric below pairs current value (clean main `9e7aafd` pre-fix
snapshot) with target value (post-Phase-22 acceptance) and the
responsible owner.

| # | Metric | Pre-fix | Target | Owner | Verifier |
|---|--------|---------|--------|-------|----------|
| 1 | Sub-3-paragraph standard cells | 107 | 0 | P22-A1 + A2 + A3 | `paragraphRollups.standardSub3Cells` |
| 2 | `depth_inversion` hits (Levenshtein ≥ 0.75) | 18 | 0 (or ≤ 2) | P22-A4 | `depthInversion.hits` |
| 3 | Brief tier PLACEHOLDER cells | 0 | 0 (hold) | P22-A1 (lift-safe) | `briefTierPlaceholder.cells` |
| 4 | Verb-form 연결 grand total | 0 | 0 (hold; P21-A1 closed) | (P21-A1 lock holds) | `yeongyeolVerb.grandTotalVerb` |
| 5 | Vulnerable `_coverage/` 연결+GYEOL_SUBS | 0 | 0 (hold) | (P20-A2 closed) | `yeongyeolCompound.coverage.vulnTotal` |
| 6 | Doubled `결이` cluster | 0 | 0 (hold) | (P20-A1 closed) | `doubledGyeoli.totalCells` |
| 7 | Post-processor leak occurrences | 0 | 0 (hold) | (P15-A2 / P18-A5 / P20-A3 ratchet) | `postProcessorLeaks.totalOccurrences` |
| 8 | `흐름이` cluster cells (≥ 3 in paragraph) | 0 | 0 (hold) | (P15-A4 closed) | `flowCluster.totalCells` |
| 9 | Hook concentration max | 15 | hold (≤ 20 enforced by gate) | (P21-A3 lock; current 15 holds) | `briefHookSurface.concentrationStats.max` |
| 10 | Hook concentration median | 10 | hold | (no Phase 22 work) | `briefHookSurface.concentrationStats.median` |
| 11 | Hook concentration min | 0 | hold (one child-age fixture intentionally bare) | (P19-A4 lock) | `briefHookSurface.concentrationStats.min` |
| 12 | Sample drift (`ci:samples-stale`) | 0 | 0 (hold) | P22-A1 (lift includes regen) + P22-A2/A3/A4 (each fragment authoring includes regen) | `ci:samples-stale` exit code |
| 13 | `ci:post-processor-grammar` `--max-violations` | 0 | 0 (hold; ratchet locked) | (P20-A3 locked) | gate exit code |
| 14 | `ci:hook-concentration` `--max-hooks=20` | PASS | PASS (hold) | (P21-A3 locked) | gate exit code |
| 15 | `ci:hook-coverage` dormant fragmentIds | 0 | 0 (hold) | (P19-A4 closed) | gate exit code |
| 16 | `test:namespring-compat` | 202/202 PASS | 202/202 PASS | (frozen contract) | exit code |
| 17 | Standard tier paragraph histogram (≥ 3) | 1598 (93.72 %) | 1705 (100 %) | P22-A1 + A2 + A3 | `paragraphHistograms.standard` keys ≥ 3 |

### Sub-row 17a — Sub-3 distribution snapshot

The 107 sub-3 cells distribute as:

| Mechanism | Count | Owner |
|---|---|---|
| `MINOR_LIMITED_CATEGORIES` early-return locks (wealth/romance/study_document × 5 periods × 5 minor fixtures) | 75 | P22-A1 (src lift) + P22-A3 (5 wealth-gap fragments) |
| `buildMinorStandardFallback` 1-paragraph emit (career/health_stress × 0-9 + health/overall × 10-19) | 32 | P22-A2 (14 fallback fragments) |

P21-A4 simulation showed Path A alone closes 52 of 75 src-locked cells
(via existing minor-gated fragments) and leaves 23 wealth-gap residue;
combined Path A + B-extended (P22-A2 + P22-A3) closes the remainder.

## Phase 22 issues observed

### I1. 107 sub-3-paragraph standard cells (target 0)

P22 closes via three coordinated tracks; depended on P22-A1 src lift
to remove the 75-cell early-return lock + P22-A3 to author 5 wealth
fragments for the 23 lift-residue cells + P22-A2 to author 14 fallback
fragments for the 32 cells (career/health_stress/health/overall) that
emit a 1-paragraph fallback today.

P22-A1's brief-tier safety: lifting the early-return opens the brief
path for minor fixtures × wealth/romance/study_document. Existing
`brief.fragments.json` for these cells must include a `gating.ageBand`
matching the fixture's band, otherwise `passesMinorGuard` rejects all
candidates and the brief tier regresses to `PLACEHOLDER_BRIEF`. The
P22-A1 measurement script `artifacts/phase22-agent-a1/measure_brief_tier.mjs`
projects the regress count; P22-A1 authors any missing brief fragments
in the same commit so brief tier maintains 0 PLACEHOLDER throughout.

### I2. 18 `depth_inversion` cells (target 0 or ≤ 2)

P21-A2 refreshed `audit-phase12.mjs` `detectDepthInversion` from raw
`plainText.length` comparison to Levenshtein similarity ≥ 0.75 on
particle-stripped, whitespace-normalized forms. Re-running against
clean main `9e7aafd` reproduces the 18-hit count.

Distribution (per P21-A2 §Hit catalog):

- **14 hits in `thisMonth/movement`** — single shared standard
  template `이번 달 이동운은 익숙한 동선에서 한 발짝씩 넓혀 가기 좋은 모습이에요`
  whose first sentence is essentially the brief headline with
  `이동운은` inserted and `좋아요` → `좋은 모습이에요` swapped.
  Affected fixtures: 02, 08, 11, 12, 14, 15, 16, 20, 21, 22, 24, 25,
  26, 35. **One fragment fix resolves all 14 cells.**

- **4 single-fixture hits**:
  - `thisYear/career` (fixture 04, similarity 0.760)
  - `thisMonth/expression_children` (fixture 29, similarity 0.870)
  - `today/overall` (fixture 22, similarity 0.800)
  - `today/academic` (fixture 25, similarity 0.762)

P22-A4 rephrases each so the standard tier's first sentence shifts
framing or focus relative to the brief headline (e.g. introduce the
secondary cadence first, then circle back to the brief's framing in
para1). brief preserves message; standard adds depth.

### I3. Brief tier PLACEHOLDER baseline (target 0; hold)

Pre-fix: 0 cells. Establishing a P22 ratchet — P22-A1's src lift opens
new brief paths; without minor-gated brief fragments those new paths
emit `PLACEHOLDER_BRIEF`. P22-A5's measurement adds the metric so
P22-A1's safety case is auditable (the lift commit must hold this
metric at 0).

## Phase 22 issues deferred / informational

### D1. 7-cell `결` density cluster carries through

Same as P14-P21-A5. 7 standard-tier cells use `결` in distinct lexical
items (`결정`, `결재`, `결실`, `한결`, deliberate parallelism).
Post-processor `reduceOverusedGyeol` only fires on the 13 GYEOL_SUB
patterns; these don't trigger. Detector retained as cross-check.

### D2. Hook concentration: max 15 hooks per fixture (held)

`13-oh-extreme-weak-continuous-tiered` has 15 distinct hooks, same as
P18/P19/P20/P21-A5 measured maximum. Median 10. P21-A3's
`--max-hooks=20` ratchet provides 5-hook headroom for normal authoring
churn. P22 fragment authoring (A2 / A3 standard-tier work) does NOT
add hooks (hooks are brief-tier only) so concentration metric holds
unchanged.

### D3. `ci:release-checklist` interactive (carry-over)

Documented exception via `tools/acceptance-manifest.json` `exempt[]`.
Not a defect.

### D4. Worktree contention persists (P19/P20/P21/P22-A5)

Four consecutive A5 audits have observed the `EnterWorktree`-claimed
isolation being yanked by parallel agents in the same checkout. P22-A5
mitigated via defensive per-gate identity tagging — every gate output
line carries `[branch=… head=…]` so any drift is auditable
post-hoc — and via `git stash push -- 'lib/spring-ts/data/'` to keep
pre-fix snapshot capture clean. The workflow-level fix (proper
isolated worktree per agent) requires harness change and is OAuth-
blocked at the repo level.

### D5. Authored hook fragility tail (carry-over from P21-A5 §Rec #7)

`wealth.thisYear.brief.balanced.neutral.004` continues at emit count 2
(lowest), 4 other fragments at emit count 4. First to fall on regression.
P21-A5 §Rec #7 recommended buffer authoring; deferred to Phase 23+.

## Recommendations for next phase (Phase 23+)

### High priority

1. **P23 — `depth_inversion` post-fix re-evaluation**. After P22-A4
   rephrases land, re-run `audit-phase12.mjs` and confirm
   `depth_inversion` hit count drops. If hits stay at 0 across multiple
   phases, consider deprecating the detector. If hits rebound (new
   templates introduce overlaps), tighten threshold to 0.65 or move
   the check into `ci:` ratchet.

2. **P23 — Workflow YAML ship**: `.github/workflows/spring-ts-samples-stale.yml`.
   Fourth-consecutive carry-over (P19/P20/P21/P22-A5 §Recommendations).
   Closes the regen-drift cycle structurally so the gate surfaces at
   PR time, not at A5 audit time. OAuth permission elevation required.

### Medium priority

3. **P23 — Worktree contention root-cause**. Four consecutive A5 audits
   observed this. Defensive identity tagging is a band-aid. Either
   `EnterWorktree` API contract or multi-agent scheduling needs revision.
   Carry-over from P19/P20/P21-A5.

4. **P23 — Sub-3 metric ratchet**. After P22-A1+A2+A3 close 107 → 0,
   add a `ci:standard-paragraph-floor --min-paragraphs=3` gate that
   asserts every standard cell emits ≥ 3 paragraphs. Prevents future
   regression without re-running the full sample suite at every PR.

5. **P23 — Brief tier PLACEHOLDER ratchet**. After P22-A1 src lift,
   add a `ci:brief-tier-placeholder --max-cells=0` gate that asserts
   `cell.brief.headline !== '준비 중인 흐름이에요.'` for every emitted
   cell. P22-A5 measurement metric `briefTierPlaceholder.cells` already
   computes the count; gate-ifying is a small `tools/check_*.mjs`
   addition.

### Low priority / informational

6. **P23 — Hook concentration baseline tracking**. P21-A3's
   `--max-hooks=20` ratchet held at 15 throughout Phase 22. Continue
   tracking median creep. If median rises from current 10 to 12+,
   indicates authoring drift toward over-broad gating.

7. **P23 — Authored hook fragility tail (carry-over P21-A5 §Rec #7)**.
   Author 1-2 sibling hook fragments for `wealth.thisYear.brief.balanced
   .neutral.004` and the 4 emit-count-4 fragments. Provides regression
   buffer.

## Files produced this phase

Owned scope (`artifacts/phase22-agent-a5/`):

| File | Purpose |
|------|---------|
| `audit-2026-05-07-phase22.md` | This document |
| `phase22-acceptance-gates.txt` | All 14 gate outputs + `ci:hook-concentration` + `test:namespring-compat` at session time, with per-gate identity tags |
| `measure_p22.mjs` | Read-only measurement (paragraphs + leak + cluster + hook + 연결X conservative + vulnerable + verb-form + hook concentration + sub-3 + depth_inversion passthrough + brief tier PLACEHOLDER) |
| `pre-fix-snapshot-2026-05-07.json` | Output of `measure_p22.mjs` against clean main `9e7aafd` |

No data, src, tools, package.json, or sample mutation. P22-A5
contributes audit / measurement artifacts only.
