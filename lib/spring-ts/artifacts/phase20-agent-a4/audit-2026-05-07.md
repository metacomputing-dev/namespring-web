# Phase 20 Agent A4 — acceptance manifest hook-coverage entry

Date: 2026-05-07
Owner: P20-A4
Scope: resolve the single `ci:acceptance-completeness` violation flagged in
P19-A5 audit §I3 by declaring the P19-A4-introduced `ci:hook-coverage`
script in `tools/acceptance-manifest.json`. Owned: manifest only.

## Headline

`ci:acceptance-completeness` returns **0 violations** post-fix. Manifest
state moves from `accepted=12 / exempt=1 / ciScripts=14 / violations=1`
to `accepted=13 / exempt=1 / ciScripts=14 / violations=0`. No existing
manifest entry was modified; only `"ci:hook-coverage"` was appended to
the `accepted[]` list.

## Decision: `accepted[]`, not `exempt[]`

Per the manifest's own `_comment_policy`, `exempt[]` is reserved for
gates that "cannot run from a worktree-local npm run (e.g. require
`GITHUB_EVENT_PATH` or PR body)". `ci:hook-coverage` is implemented as
`node tools/check_hook_coverage.mjs --max-violations=0` with no
environment dependencies and runs cleanly from a worktree (verified
PASS in P19-A5 audit §"Acceptance gates run", row 13: "34/34 authored,
0 dormant by fragmentId, 2-emit fragility tail visible"). It belongs
in `accepted[]`.

## Diff

```diff
--- a/lib/spring-ts/tools/acceptance-manifest.json
+++ b/lib/spring-ts/tools/acceptance-manifest.json
@@ -13,7 +13,8 @@
     "ci:narrative-orphan-tags",
     "ci:acceptance-completeness",
     "ci:samples-stale",
-    "ci:post-processor-grammar"
+    "ci:post-processor-grammar",
+    "ci:hook-coverage"
   ],
```

Append-only; preserves order of all prior entries. The check tool sorts
internally (`tools/check_acceptance_completeness.mjs:80,101`) so trailing
position is functionally equivalent to alphabetic insertion.

## Validation

| Gate | Status | Detail |
|------|--------|--------|
| `npm run typecheck` | PASS | tsc --noEmit, no errors |
| `npm run ci:acceptance-completeness` | PASS | ciScripts=14, accepted=13, exempt=1, violations=0 |
| `npm run ci:release-checklist` | n/a | Errors with "PR body is required" — expected `exempt[]` behavior, not in P20-A4 scope |
| `npm run test:namespring-compat` | PASS | 202 / 0 |

## Cross-references

- P19-A4 commit `0d6675b tools+ci(p19-a4): brief.hook coverage CI gate`
  introduced the `ci:hook-coverage` script and `tools/check_hook_coverage.mjs`.
- P19-A5 audit `artifacts/phase19-agent-a5/audit-2026-05-07-phase19.md`
  §I3 ("`ci:acceptance-completeness` is in transient FAIL state") flagged
  the manifest entry as the resolution path and §"Phase 19 acceptance
  gate definition" row 13 set "P19-A4 PR manifest update" as the owner;
  P20-A4 closes that follow-up.
