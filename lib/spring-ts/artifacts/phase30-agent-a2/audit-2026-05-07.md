# P30-A2 — Counterexample/contrast 다양화 audit (health/family/movement)

> 작성: 2026-05-07
> Owned scope: `data/narrative/{health,family,movement}/<period>/standard.fragments.json` 의
> paragraph 내부 contrast/negation 절 ("X 가 아니라 Y", "X 보다 Y", "X 라기보다 Y") 만.
> 다른 paragraph 본문, livingTips, cautions, 다른 카테고리 무수정.

## 1. 스코프

P28-A1 (academic/career/overall/study_document/wealth) 의 후속 — 같은 contrast clause 다양화
원칙을 health/family/movement 3 카테고리에 적용. P28-A1 audit § 1 에서 정리한 paragraph
내부 대비 절 기준 적용 (`templateTokens[].kind=text` 의 `value` 안의 `~보다`/`~기보다`/
`~보다는`/`가 아니라`/`이 아니라`/`라기보다` 패턴).

## 2. 사전 audit

`[가-힣 ]{2,15}(보다는?|가 아니라|이 아니라|라기보다|기보다)` 매치.

| Metric | Before | After |
|---|---|---|
| Total contrast phrases (3 cats × 5 periods) | 95 | 87 |
| Top cluster (6 occ, cross-trunk) | 1 | 0 |
| 4-occurrence clusters | 1 | 0 |
| 3-occurrence clusters | 0 | 0 |

가장 반복이 심했던 클러스터:

| 반복 | 핵심 phrase | 분포 |
|---|---|---|
| 6 (cross-trunk) | `큰 일을 새로 시작하기보다는, 미뤄 둔 안부를 한 사람씩 챙기는` | family.aligned.009 × 4 + family.wildcard.001 × 2 (사실상 두 trunk 가 동일 phrase 공유) |
| 4 | `큰 일정을 잡기보다는, 매일·매주의 작은 결을 따뜻하게 누리는` | family.elder.008 × 4 (life 는 변형) |
| 4 (cross-period) | `무리한 자리 변경보다 익숙한 자리에서 시야를 넓히는 결이 결실에 가까워요` | movement.40_54.006 thisYear/life (byte-identical pair) |
| 5 | `무리한 X보다 / 갑작스러운 큰 변화보다` | movement.weak_neutral.010 × 5 periods (variant phrase) |
| 3 | `무리한 X보다 (외출 / 장거리 / 장거리)` | movement.70plus.008 × 3 |
| 4 | `무리(해서) 화해(를 서두르기)보다(는)` | family.conflicting.010 × 4 (life 제외) |

## 3. 변경 전략

P28-A1 anchor 규칙 따름: `today` (또는 첫 period) 는 canonical 으로 보존, 나머지 period
diversification. cross-trunk 공유 phrase (family aligned.009 ↔ wildcard.001) 는 두 trunk
모두에 서로 다른 paraphrase 분배.

다양화 옵션 mix:
1. `X 라기보다 Y` (introduce: P30-A2 도입 전 health/family/movement 스코프 = 0회)
2. `X 가 아니라 Y` / `X 이 아니라 Y`
3. `X 대신 Y`
4. `X 욕심내기보다 Y` / `X 욕심을 내지 않고 Y` / `X 욕심내지 않고 Y`
5. `굳이 X 하지 않고 Y`
6. `X 에 마음을 두지 말고 Y` / `X 하려 하지 말고 Y`
7. positive reframe (`결이 아니라`)

## 4. 변경 fragment (15)

| # | fragmentId | 변경 전 → 변경 후 |
|---|---|---|
| 1 | family.thisMonth.standard.elder.008 | `큰 일정을 잡기보다는, 매일·매주의 작은 결을 따뜻하게 누리는` → `굳이 큰 일정을 잡지 않고, 매일·매주의 작은 결을 따뜻하게 누리는` |
| 2 | family.thisYear.standard.elder.008 | `큰 일정을 잡기보다는, 매주·매달의 작은 결을 따뜻하게 누리는` → `큰 일정을 한 해에 몰아 잡는 결이 아니라, 매주·매달의 작은 결을 따뜻하게 누리는` |
| 3 | family.life.standard.elder.008 | `큰 일을 새로 시작하는 자리이기보다는, 매일의 작은 결을` → `큰 일을 새로 시작하는 자리라기보다, 매일의 작은 결을` (라기보다 #1 도입) |
| 4 | family.thisWeek.standard.aligned.009 | `큰 일을 새로 시작하기보다는, 미뤄 둔 안부를` → `큰 일을 한 주에 새로 시작하는 대신, 미뤄 둔 안부를` |
| 5 | family.thisMonth.standard.aligned.009 | 같은 cluster → `큰 일을 새로 시작할 욕심을 내지 않고, 미뤄 둔 안부를` |
| 6 | family.thisYear.standard.aligned.009 | 같은 cluster → `큰 일을 한 해의 시작에 욕심내기보다, 미뤄 둔 안부를` |
| 7 | family.thisWeek.standard.wildcard.001 | (cross-trunk 공유) → `큰 일을 한 번에 새로 시작하는 자리에 마음을 두지 말고, 미뤄 둔 안부를` |
| 8 | family.thisMonth.standard.wildcard.001 | (cross-trunk 공유) → `큰 일을 한꺼번에 새로 시작하는 자리가 아니라, 미뤄 둔 안부 자리를` |
| 9 | family.thisYear.standard.conflicting.010 | `무리한 화해보다는 작은 결이 더 단단해요` → `한 번에 매듭짓는 화해라기보다, 작은 결이 모이며 더 단단해져요` (라기보다 #2 도입) |
| 10 | health.thisMonth.standard.child.001 | `한 번에 너무 많이 바꾸기보다 한두 가지만 더해 주면` → `한 번에 너무 많이 바꾸려 하지 말고, 한두 가지만 더해 주면` |
| 11 | health.thisYear.standard.child.001 | `무리한 일정보다 익숙한 놀이·익숙한 음식이` → `무리한 일정 대신 익숙한 놀이·익숙한 음식이` |
| 12 | health.life.standard.child.001 | `한 번에 큰 변화를 주기보다 매일 조금씩 받쳐 주면` → `한 번에 큰 변화를 욕심내지 않고, 매일 조금씩 받쳐 주면` |
| 13 | movement.thisMonth.standard.weak_neutral.010 | `갑작스러운 한 번의 큰 변화보다 활동 범위를 천천히` → `갑작스러운 한 번의 큰 변화가 아니라, 활동 범위를 천천히` |
| 14 | movement.thisYear.standard.40_54.006 | `무리한 자리 변경보다 익숙한 자리에서 시야를 넓히는` → `무리한 자리 변경 대신 익숙한 자리에서 시야를 넓히는` |
| 15 | movement.life.standard.40_54.006 | (thisYear 와 byte-identical 이었음) → `자리를 무리해서 바꾸려 하기보다, 익숙한 자리에서 시야를 넓히는` (#14 와 다른 paraphrase) |

총 15 fragments. anchor (`today` 또는 첫 period) 는 모두 보존. cross-trunk 공유 cluster
(aligned.009 ↔ wildcard.001) 는 두 trunk 모두에 서로 다른 phrase. byte-identical pair
(40_54.006 thisYear+life) 는 각기 다른 phrase 로 다양화.

## 5. 효과

- 6-occurrence cross-trunk cluster (큰 일을 새로 시작하기보다는…) → 0
- 4-occurrence clusters (family.elder.008, family.aligned.009 단독, family.conflicting.010,
  movement.40_54.006 byte-identical pair) → 모두 분리
- Suffix 분포 변화:

| Suffix | Before | After |
|---|---:|---:|
| `보다` | 40 | 36 |
| `기보다` | 41 | 33 |
| `보다는` | 7 | 6 |
| `가 아니라` | 3 | 5 |
| `이 아니라` | 4 | 5 |
| `라기보다` | 0 | 2 (신규 도입) |

## 6. CI 검증 결과

| Gate | Result |
|---|---|
| `npm run typecheck` | PASS |
| `npm run ci:no-ai-policy` | PASS (fixtureFiles=429, sourceTierRecords=7163) |
| `npm run ci:narrative-voice` | PASS (4145 fragments, 0 violations) |
| `npm run ci:narrative-truncated-endings` | PASS (filesScanned=344, 0) |
| `npm run test:narrative-schema:summary` | PASS (104343 / 0 FAIL) |
| `npm run test:namespring-compat` | PASS 202/202 |
| `npm run samples:regen` + `npm run ci:samples-stale` | PASS (37 files, 0 violations after regen) |

## 7. Sample regen 주의사항

`ci:samples-stale` 은 P30-A2 narrative 편집 전 baseline (`ef84f66d`) 에서 이미 15 violations
(generatedAt 마스킹 후에도 score/safetyProfile 등 차이) 있었음. 이는 saju-ts/dist 의 비결정적
빌드 환경 차이 (이전 regen 065821c1 = P29-A4 의 빌드 환경 vs P30-A2 작업 환경) 에서 누적된
drift 로 추정. P30-A2 본 편집은 drift 위에 narrative 변경 효과만 추가 (~16 file 추가
divergence, 총 ~31 violations).

본 P30-A2 의 sample regen commit 은:
- P30-A2 narrative 변경 (15 fragments) 의 sample 측 반영 + 기존 환경 drift 동시 흡수
- 분리 commit (data → samples → audit) 으로 reviewer 가 narrative 변경과 sample 변경을
  구분 가능하도록 구성
- score/safetyProfile 차이는 saju-ts dist 환경 결정성 외부 영역이라 본 phase 에서 진단·수정
  하지 않음 (스코프: narrative paragraph contrast 절만)

## 8. Out-of-scope finding (다음 phase 권고)

samples-stale baseline drift 15 violations: saju-ts/dist 비결정적 빌드 환경 의심. P29-A4
(065821c1) 의 sample regen 이후 narrative/saju-ts 변경 없는데도 같은 머신에서 다시 regen
시 score/safetyProfile 차이 발생. 환경 결정성 audit (Node version pin, npm ci, 빌드 OS
documentation 등) 권고.

## 9. 결론

- P28-A1 의 counterexample 다양화 패턴을 health/family/movement 3 카테고리에 확장 적용
- 가장 큰 cluster (cross-trunk 6x family.aligned.009 ↔ wildcard.001) 해소
- byte-identical pair (movement.40_54.006 thisYear/life) 분리
- `라기보다` 신규 도입 (0 → 2) 등 어조 분포 다양화
- 18 CI gates / 자체 테스트 0 violations 유지, namespring-compat 202/202 hold
