# Phase 26 Agent A5 — Comprehensive audit + acceptance measurement

Date: 2026-05-07
Owner: P26-A5
Branch: `phase26-agent-a5`
Base HEAD at audit time: `f7b8dc1f` (`docs(plan): Phase 26 narrative breadth expansion`)
Scope: Phase 26 comprehensive audit. P26-A5 is measurement-only — no
mutation to `data/`, `src/`, `tools/`, `package.json`, `../namespring/`,
`../saju-ts/src/`. Owned scope: `artifacts/phase26-agent-a5/` only.

## Headline

**18 / 18 production CI gates PASS at HEAD `f7b8dc1f`.**

The single Phase 25 carry-over (`ci:samples-stale` 4 violations) was
**closed by Phase 25 sample regen commit `29393b7b`**
(`artifacts(phase3-samples): regenerate samples (restore post-P24-A3
state)` — attributed to P25-A1 scope per
`PLAN_PHASE26_BREADTH_EXPANSION.md` §0). The committed samples now match
the `data/`-layer post-P24-A3 state, eliminating the gyeolCluster lock
divergence as well. P25-A2's 4p-band lift (12 fragments 3p → 4p) is also
visible at this HEAD.

`test:namespring-compat` continues at 202 / 0 PASS. Both Phase 24
ratchet gates (`ci:standard-paragraph-floor`, `ci:brief-tier-placeholder`)
PASS. Verb-form 연결, vulnerable 연결+GYEOL_SUBS, post-processor leak,
doubled `결이`, hook concentration max=15, depth_inversion=0,
audience_leak=0 all hold at the Phase 25 post-fix baseline.

| Lock | Value at `f7b8dc1f` | Target | Status |
|------|--------------------:|--------|--------|
| Sub-3 standard cells | 0 | 0 | HOLD (P24 ratchet) |
| Brief tier PLACEHOLDER cells | 0 | 0 | HOLD (P24 ratchet) |
| `depth_inversion` hits | 0 | 0 (or ≤ 2) | HOLD |
| Verb-form 연결 grand total | 0 | 0 | HOLD (P21 lock) |
| Vulnerable `_coverage/` 연결+GYEOL_SUBS | 0 | 0 | HOLD (P20 lock) |
| Doubled `결이` cluster | 0 | 0 | HOLD (P20 lock) |
| Post-processor leak occurrences | 0 | 0 | HOLD (P15/P18/P20 lock) |
| `흐름이` cluster cells | 0 | 0 | HOLD (P15 lock) |
| Hook concentration max / median / min | 15 / 10 / 0 | hold (≤ 20) | HOLD (P21 lock) |
| `ci:samples-stale` violations | 0 | 0 | **CLOSED** (P25-A1 regen) |
| `ci:hook-coverage` dormant fragmentIds | 0 | 0 | HOLD (P19 lock) |
| `audit-phase12.mjs` audience_leak | 0 | 0 | HOLD (P24-A4 close) |
| `test:namespring-compat` | 202 / 0 | 202 / 0 | HOLD |
| `gyeolCluster.totalCells` | **9** | 9 (P24-A3 lock) | **MET** (P25-A1 close) |

Concurrent Phase 26 sibling agents (A1/A2/A3/A4) are **not visible** at
HEAD `f7b8dc1f` — only the `docs(plan)` commit has merged. Phase 26
sibling delivery targets (4p +20, 5p +10, distinct hook +8, hook safety
margin) are therefore **not measurable yet**; the Phase 26 acceptance
verdict for those targets is "pre-sibling-merge baseline captured".
Their merges will need a follow-up snapshot.

| Agent | Task | Status visible at HEAD `f7b8dc1f` |
|-------|------|------------------------------------|
| P26-A1 | Hook safety margin (sibling brief.hook authoring) | Not yet merged |
| P26-A2 | Standard 4p band +20 (95 → ~115) | Not yet merged |
| P26-A3 | 5p band +10 (237 → ~247) | Not yet merged |
| P26-A4 | Brief hook 34 → ~42 (+8) | Not yet merged |

## Workflow

P26-A5 ran in an isolated worktree at `C:/Projects/metaintelligence/p26-a5-iso`
(P25-A5 pattern) to avoid cross-agent contention with P26-A1 / P26-A2 /
P26-A3 / P26-A4 mutating `data/` and `artifacts/sample-outputs-...` in
parallel.

1. `git worktree add -b phase26-agent-a5 <iso-path> f7b8dc1f`.
2. `npm install` in `lib/saju-ts`, `lib/spring-ts`, `lib/seed-ts`
   (fresh worktree had no `node_modules`).
3. `npm --prefix ../saju-ts run build` — clean
   `tsc -p tsconfig.build.json` (saju-math-engine@0.34.3 OK).
4. `npm --prefix . run build` — clean `tsc` (spring-ts@1.0.0 OK).
5. `node artifacts/phase23-agent-a4/measure_p23.mjs
   artifacts/phase26-agent-a5/post-fix-snapshot-2026-05-07.json` — uses
   the same mtime-aware corpus refresh script P23-A4 authored. Refresh
   fired with `reason=samples-newer-than-corpus` (consistent with
   P23-A5 / P24-A5 / P25-A5).
6. `node artifacts/phase24-agent-a3/find-gyeol-cluster.mjs >
   artifacts/phase26-agent-a5/gyeol-cluster-cells-now.json` — captures
   the 9 cells at audit time (matches P24-A3 close claim).
7. 18 production CI gates (`typecheck` + 16 `ci:*` + `test:namespring-compat`)
   invoked with per-gate identity tag
   `[branch=phase26-agent-a5 head=f7b8dc1f]` captured to
   `phase26-acceptance-gates.txt`.

## Pre-fix vs post-fix metric table

Pre-fix column = P25-A5 post-fix at `1213c71f`
(`artifacts/phase25-agent-a5/post-fix-snapshot-2026-05-07.json`),
which is the carry-in baseline for Phase 26.
Post-fix column = this audit at `f7b8dc1f`
(`artifacts/phase26-agent-a5/post-fix-snapshot-2026-05-07.json`).

The Phase 25 sibling work (P25-A1 sample regen + P25-A2 4p-band lift)
landed between P25-A5's audit and this one — visible as
`gyeolCluster.totalCells: 12 → 9`,
`samples-stale: 4 → 0 violations`, and standard histogram
`3p: 1385 → 1373 (−12)`, `4p: 83 → 95 (+12)`.

| # | Metric | Pre-fix (P25-A5 @ `1213c71f`) | Target | Post-fix (P26-A5 @ `f7b8dc1f`) | Status |
|---|--------|-------------------------------|--------|---------------------------------|--------|
| 1 | Sub-3-paragraph standard cells | 0 | 0 (hold) | 0 | HOLD |
| 2 | Brief tier PLACEHOLDER cells | 0 | 0 (hold) | 0 | HOLD |
| 3 | `depth_inversion` hits (Levenshtein ≥ 0.75) | 0 | 0 (or ≤ 2) | 0 | HOLD |
| 4 | Verb-form 연결 grand total | 0 | 0 (hold) | 0 | HOLD |
| 5 | Vulnerable `_coverage/` 연결+GYEOL_SUBS | 0 | 0 (hold) | 0 | HOLD |
| 6 | Doubled `결이` cluster | 0 | 0 (hold) | 0 | HOLD |
| 7 | Post-processor leak occurrences | 0 | 0 (hold) | 0 | HOLD |
| 8 | `흐름이` cluster cells (≥ 3 in paragraph) | 0 | 0 (hold) | 0 | HOLD |
| 9 | Hook concentration max | 15 | hold (≤ 20) | 15 | HOLD |
| 10 | Hook concentration median | 10 | hold | 10 | HOLD |
| 11 | Hook concentration min | 0 | hold | 0 | HOLD |
| 12 | Sample drift (`ci:samples-stale`) | 4 | 0 (target) | 0 | **CLOSED** (P25-A1) |
| 13 | `ci:post-processor-grammar` `--max-violations` | 0 | 0 (hold) | 0 | HOLD |
| 14 | `ci:hook-concentration` `--max-hooks=20` | PASS | PASS (hold) | PASS | HOLD |
| 15 | `ci:hook-coverage` dormant fragmentIds | 0 | 0 (hold) | 0 | HOLD |
| 16 | `test:namespring-compat` | 202/202 PASS | 202/202 | 202/202 | HOLD |
| 17 | Standard tier paragraph histogram (≥ 3) | 1705 (100 %) | 1705 (100 %) | 1705 (100 %) | HOLD |
| 18 | `gyeolCluster.totalCells` | 12 | 9 (P24-A3 lock) | 9 | **CLOSED** (P25-A1) |
| 19 | `audience_leak` (audit-phase12) | 0 | 0 (hold) | 0 | HOLD |
| 20 | Distinct hook strings emitted | 34 | hold | 34 | HOLD |
| 21 | Authored hook strings | 34 | hold | 34 | HOLD |
| 22 | Lowest emit-count fragility tail | `wealth.thisYear.brief.balanced.neutral.004` (=2); 4 at =4 | hold | same fragment (=2); 4 at =4 | UNCHANGED |
| 23 | `dormantHookStringsByText` | 1 | hold or close | 1 | HOLD (`career.thisWeek.brief.age40plus.009`) |
| 24 | `ci:acceptance-completeness` ciScripts / accepted / exempt | 17 / 16 / 1 | hold | 17 / 16 / 1 | HOLD |

### Standard paragraph histogram delta (P25-A2 4p-band lift visible)

| paragraphCount | Pre-fix cells (P25-A5) | Post-fix cells (P26-A5) | Delta |
|---|---|---|---|
| 1 | 0 | 0 | 0 |
| 2 | 0 | 0 | 0 |
| 3 | 1385 (81.23 %) | 1373 (80.53 %) | **−12** |
| 4 | 83 (4.87 %) | 95 (5.57 %) | **+12** |
| 5 | 237 (13.90 %) | 237 (13.90 %) | 0 |
| **Total** | **1705** | **1705** | 0 |

P25-A2's planned "4p band +12" merged at commit `0959f71f`
(`data(narrative): P25-A2 lift 12 standard fragments 3p -> 4p`).
The histogram now shows the expected −12/+12 shift between 3p and 4p
bands, with sub-3 still at 0 (P24-A1 ratchet preserved). 5p band is
unchanged at 237.

P26-A2's planned 4p +20 (95 → ~115) and P26-A3's 5p +10 (237 → ~247)
are **not yet visible** — siblings unmerged at audit HEAD.

## Phase 26 acceptance gate results

### 18 production CI gates run at HEAD `f7b8dc1f`

P24-A1 (`ci:standard-paragraph-floor`) and P24-A2
(`ci:brief-tier-placeholder`) ratchets remain merged. Gate count is
18 (matches P25-A5). All 18 PASS at this audit.

| Gate | Status | Note |
|------|--------|------|
| `typecheck` | PASS | tsc --noEmit, no errors |
| `ci:no-ai-policy` | PASS | fixtureFiles=429, sourceTierRecords=7137 |
| `ci:narrative-voice` | PASS | 0/0/0 violations |
| `ci:narrative-truncated-endings` | PASS | filesScanned=344, 0 violations |
| `ci:narrative-tag-label-alignment` | PASS | 0 missing/mismatch |
| `ci:narrative-orphan-tags` | PASS | glossaryLabels=194, filesScanned=344, fragmentsScanned=3954, 0 violations |
| `ci:narrative-cell-axis` | PASS | 0 missing |
| `ci:narrative-density` | PASS | 8 pairs covered, 0 thin/deficit |
| `ci:narrative-tuple-density` | PASS | 3 tuples covered, 0 missing/thin/deficit |
| `ci:narrative-daymaster-tuple-density` | PASS | 1 tuple covered, 0 missing/thin/deficit |
| `ci:post-processor-grammar` | PASS | filesScanned=32, violations=0 (max=0 lock) |
| `ci:hook-coverage` | PASS | 0 dormant by slot; 0 dormant by hook text |
| `ci:hook-concentration` | PASS | max=15 vs threshold=20 (5-hook headroom) |
| `ci:standard-paragraph-floor` | PASS (P24-A1 ratchet) | min=3 violations=0 |
| `ci:brief-tier-placeholder` | PASS (P24-A2 ratchet) | violations=0, fixturesSkipped=4 (no tieredMatrix) |
| `ci:acceptance-completeness` | PASS | violations=0; ciScripts=17, accepted=16, exempt=1 |
| `ci:samples-stale` | **PASS** (closed) | sampleFiles=37, violations=0 |
| `test:namespring-compat` | PASS | 202 / 0 |

**18 / 18 gates PASS, 0 / 18 FAIL.** Per-gate output captured to
`phase26-acceptance-gates.txt`.

## Phase 25 carry-over closure verification

### `ci:samples-stale` 4 violations → 0 violations

P25-A5 §B noted 4 reproducible drift violations at fixtures 05/17/33/
`index.json`, traced to a build-environment / `dist/` non-determinism
issue. Closure path proposed: regenerate samples from a build
environment matching the main worktree's dist, then commit the regen.

Phase 25 commit `29393b7b` (`artifacts(phase3-samples): regenerate
samples (restore post-P24-A3 state)`) executed exactly that closure —
attributed to P25-A1 scope per `PLAN_PHASE26_BREADTH_EXPANSION.md` §0.
At this audit:

```
Samples stale: PASS
  sampleFiles=37
  violations=0
```

### `gyeolCluster.totalCells = 12 → 9` (P24-A3 lock now realized)

P24-A3 closed `gyeolCluster.totalCells = 9` at the data layer in
`c75bb9c1` but the rendered samples still embedded the pre-paraphrase
prose (12 cells observed at P22-A6 / P23-A5 / P24-A5 / P25-A5).
With the Phase 25 sample regen at `29393b7b`, the rendered artifacts
now reflect the post-paraphrase data. This audit measures **9 cells**,
matching the P24-A3 lock claim.

`gyeol-cluster-cells-now.json` (this phase) lists those 9 cells:
- 5 × `wealth.thisMonth.standard` (4 / 12 / 14 / 21 / 26 fixtures)
- 4 × `career.today.standard` (10 / 11 / 13 / 15 fixtures)

These match the P24-A3 hold list — no new entries surface.

## Phase 26 acceptance gate (sibling delivery targets)

The Phase 26 plan (`PLAN_PHASE26_BREADTH_EXPANSION.md`) sets four
sibling delivery targets. Their measurability requires sibling merges,
which have **not landed** at HEAD `f7b8dc1f`.

| Phase 26 target | Pre-merge baseline (this audit) | Sibling target | Status |
|---|---|---|---|
| P26-A1 — Hook safety margin (6-9 sibling brief.hook fragments in family.today / romance.thisYear / study_document.today) | 4 fragments at emit=4 in those slots; 0 sibling additions | 6-9 sibling fragments authored | Not yet merged |
| P26-A2 — Standard 4p band +20 (95 → ~115) | 4p band = 95 cells | 4p band ~115 (+20) | Not yet merged |
| P26-A3 — Standard 5p band +10 (237 → ~247) | 5p band = 237 cells | 5p band ~247 (+10) | Not yet merged |
| P26-A4 — Brief hook 34 → ~42 (+8) | distinctHookStrings = 34, authored = 34 | distinct ~42, authored ~42 (+8) | Not yet merged |

Phase 26 acceptance verdict: **pre-sibling-merge audit baseline captured.
Sibling delivery targets not yet measurable.** A follow-up snapshot will
be needed once P26-A1 / A2 / A3 / A4 merge.

### Sibling-merge predicted impact (informational, for cross-check after merge)

Assuming all four siblings merge with their stated targets:

- **Standard histogram** would shift: 3p ~1373 → ~(1353), 4p ~95 →
  ~115 (+20 from A2 only; A3 lifts 4p → 5p so net 4p = 95 + 20 − 10 =
  105 in net), 5p ~237 → ~247 (+10 from A3). Note the inter-sibling
  arithmetic depends on A2 and A3 not overlapping — if they touch the
  same fragments the net may differ.
- **Hook surface** would shift: distinct ~34 → ~42 (+8 from A4); emit
  count tail (currently `family.today.brief.young.003` etc at =4)
  would distribute across new siblings (A1 effect).
- **Hook concentration max** could rise from 15 toward ~17 with new
  hooks; threshold = 20 still gives 3-hook headroom (the plan §A4
  notes this is acceptable).
- **Authored hook strings**: if all 4 hooks fragments from A1 carry
  distinct hook text and A4 adds 8 new fragments with new hooks,
  authored ~34 + ~6 (A1) + 8 (A4) = ~48. The "~42" plan target
  for A4 assumes A4 in isolation; the post-merge total likely
  approaches ~48.

These are projections only; the post-merge snapshot will measure
ground truth.

## Hold-target metrics (regression-free across Phase 26 audit)

The following Phase 18-25 acceptance gates remain at their P25-A5
baseline:

- Verb-form 연결 grand total: 0 (P21-A1 lock)
- Vulnerable `_coverage/` 연결+GYEOL_SUBS: 0 (P20-A2 lock)
- Doubled `결이` cluster: 0 (P20-A1 lock)
- Post-processor leak occurrences: 0 (P15-A2 / P18-A5 / P20-A3 lock)
- `흐름이` cluster cells: 0 (P15-A4 lock)
- Hook concentration max=15 / median=10 / min=0 (P21-A3 lock)
- `ci:post-processor-grammar` `--max-violations`: 0 (P20-A3 lock)
- `ci:hook-concentration` `--max-hooks=20`: PASS
- `ci:hook-coverage` dormant fragmentIds: 0 (P19-A4 lock)
- `ci:standard-paragraph-floor` `--min-paragraphs=3`: 0 violations (P24-A1)
- `ci:brief-tier-placeholder` `--max-cells=0`: 0 violations (P24-A2)
- `audit-phase12.mjs` audience_leak: 0 (P24-A4 close)
- `test:namespring-compat`: 202/202 PASS

## Phase 25 → Phase 26 carry-over residues

Listed for tracking continuity.

1. **`samples-stale` 4 → 0 violations** (P25-A1 closed). DONE.

2. **`gyeolCluster.totalCells = 12 → 9` realized** (P25-A1 sample
   regen). DONE — matches P24-A3 lock claim.

3. **Authored hook fragility tail** (P25-A5 §Rec #4 carry-over):
   - 1 dormant (emit count 0): `career.thisWeek.brief.age40plus.009`
     `쌓아 온 결이 자리를 받쳐 줘요.` (counted as
     `dormantHookStringsByText: 1` in snapshot)
   - 1 at emit count 2: `wealth.thisYear.brief.balanced.neutral.004`
     (UNCHANGED from P24-A5 / P25-A5)
   - 4 at emit count 4 (P25-A5 listed 4 — UNCHANGED at this audit's
     gate output):
     - `family.today.brief.young.003`
     - `health.today.brief.strong.elder.001`
     - `romance.thisYear.brief.midlife.001`
     - `study_document.today.brief.adult.005`

   Note: P25-A5 §Rec listed 7 fragments at emit=4 in a "full ≤ 4
   sweep". The gate's printed-5 output continues to match the lowest
   tail (5 fragments). The discrepancy was P25-A5's full-sweep
   diagnostic vs the gate's lowest-5 view; both views remain accurate.
   P26-A1 explicitly targets 3 of these 4 emit=4 slots (family.today,
   romance.thisYear, study_document.today) for sibling authoring.

4. **Workflow YAML ship**:
   `.github/workflows/spring-ts-samples-stale.yml` (ninth consecutive
   carry-over P19/P20/P21/P22-A5/A6/P23-A5/P24-A5/P25-A5/P26-A5
   §Recommendations). The Phase 25 plan §3 still calls this out;
   P25-A4's audit trail records continued blocked-on-OAuth status.
   No agent has merged the YAML at this HEAD.

## Corpus freshness verification (P22-A6 §Rec #5 → P23-A4 closed)

`measure_p23.mjs` reports:

```json
{
  "corpusExisted": true,
  "corpusMtimeBefore": 1778146847638,
  "maxSampleMtime":   1778146848440,
  "maxSampleMtimeFile": "diff-legacy-vs-tiered.json",
  "extractScriptMtime": 1778146847614,
  "refreshed": true,
  "reason": "samples-newer-than-corpus",
  "refreshError": null,
  "corpusMtimeAfter": 1778146948302
}
```

P23-A4's mtime-aware corpus refresh continues to fire automatically.
`depth_inversion` reads the post-regen corpus, eliminating the false
18-hit reading P22-A6 hit on first run. This is a hold from P23-A5 /
P24-A5 / P25-A5; P26 does not re-investigate.

## Phase 26 acceptance summary

| Acceptance criterion | Status |
|---|---|
| Sub-3 standard cells = 0 (P24 ratchet) | MET (= 0) |
| Brief PLACEHOLDER = 0 (P24 ratchet) | MET (= 0) |
| `depth_inversion` ≤ 2 (hold) | MET (= 0) |
| `audience_leak = 0` (P24-A4 close) | MET (= 0) |
| Corpus freshness automated (P22-A6 §Rec #5) | MET (P23-A4 fix) |
| All 18 production CI gates PASS | **MET — 18/18 PASS** |
| `test:namespring-compat` 202/202 | MET |
| Hold targets (verb-form / cluster / leak / hook concentration) | MET |
| `gyeolCluster = 9` lock (P24-A3) | **MET** (P25-A1 close at `29393b7b`) |
| `ci:samples-stale = 0` violations | **MET** (P25-A1 close at `29393b7b`) |
| Phase 26 sibling delivery targets (4p +20, 5p +10, hook +8, hook safety) | NOT MEASURABLE (siblings unmerged at audit HEAD) |

**Phase 26 audit verdict**: All Phase 18-25 invariants hold at the data
layer **and** the rendered-artifact layer. The Phase 25 build-determinism
gap closed by P25-A1's sample regen — `gyeolCluster = 9` lock is now
realized in committed samples; `ci:samples-stale = 0` is restored. P25-A2's
4p-band lift (+12) is visible in the standard histogram. The 18-gate
production CI surface PASSES end-to-end with no regressions. Phase 26
sibling delivery work has not yet merged; sibling targets will be measured
in a follow-up snapshot.

## Recommendations for Phase 26 close + Phase 27+

### High priority

1. **P26 close — Post-sibling-merge follow-up audit**.
   Once P26-A1 / A2 / A3 / A4 merge, capture a new snapshot and re-run
   the 18 gates to verify (a) sibling targets met, (b) no regression
   in hold metrics, (c) hook concentration max stays ≤ 20. The
   sibling-merge predicted impact §above provides cross-check ground
   truth. Recommended as a final P26 follow-up commit (`P26-A5 follow-up`).

### Medium priority

2. **P27 — Workflow YAML ship**:
   `.github/workflows/spring-ts-samples-stale.yml`. **Ninth
   consecutive carry-over** (P19/P20/P21/P22-A5/A6/P23-A5/P24-A5/
   P25-A5/P26-A5 §Recommendations). The recommendation has not
   advanced because workflow YAML changes require OAuth permission
   elevation that no single agent has. Suggest a maintainer-owned PR
   pinned to one commit per phase to drain this carry-over.

3. **P27 — Build-determinism investigation closure**.
   P24-A4 §"Build-environment note" first surfaced the dist-divergence
   between the main shared worktree and a freshly-built sibling
   worktree. P25-A5 confirmed the divergence is reproducible. P25-A1's
   sample regen closed the **symptom** (drift) but the **root cause**
   (which dist state diverges, why) is still uninvestigated. A Phase
   27 `dist/` deterministic-replay audit would (a) identify what state
   in the main worktree's dist diverges (TS incremental cache? on-disk
   artifact ordering?), (b) document the "canonical regen procedure"
   that all agents should follow, and (c) optionally add a
   `ci:dist-determinism` gate that compares two sequential clean
   rebuilds.

### Low priority (continued from P25-A5 §Rec)

4. **P27 — Authored hook fragility tail expansion (post-A1 follow-up)**.
   After P26-A1 lands, re-measure the emit ≤ 4 tail. P26-A1 covers 3
   of the 4 emit=4 slots (family.today / romance.thisYear /
   study_document.today). The remaining tail will be:
   - 1 dormant: `career.thisWeek.brief.age40plus.009` (still uncovered;
     no Phase 26 sibling addresses it — Phase 27 candidate)
   - 1 at emit=2: `wealth.thisYear.brief.balanced.neutral.004` (still
     uncovered)
   - 1 at emit=4: `health.today.brief.strong.elder.001` (not in P26-A1
     plan)

   Phase 27 could close the dormant fragment (highest leverage —
   emit=0 means it never selects under any fixture) and the
   `wealth.thisYear` and `health.today` slots that P26-A1 doesn't
   touch. 3 sibling fragments would close the post-P26-A1 tail to
   ≥ 5 emit count uniformly.

### Phase 27+ phase-shape options (longer horizon)

5. **Brief tier richness expansion (continued from P25-A5 §Rec #6)**.
   With P26-A4 lifting authored count 34 → ~42 and P26-A1 adding
   ~6 sibling hooks, post-P26 authored hook count will be ~48. Phase
   27 could push to ~60 and lift hook concentration max from 15 toward
   the threshold ceiling of 20 by introducing more variety per fixture
   axis.

6. **Standard tier 5p band growth**.
   With P26-A3 lifting 5p band 237 → ~247, the band remains ~14 % of
   1705 cells. Phase 27+ could target ~20 % (≈ 340 cells) by lifting
   ~90 fragments from 4p → 5p over 3-4 phases, balancing per-phase
   commit ≤ 300 LOC constraint.

## Files produced this phase

Owned scope (`artifacts/phase26-agent-a5/`):

| File | Purpose |
|------|---------|
| `audit-2026-05-07.md` | This document |
| `phase26-acceptance-gates.txt` | All 18 production CI gates, each with per-gate identity tag |
| `post-fix-snapshot-2026-05-07.json` | Output of `measure_p23.mjs` against current main HEAD `f7b8dc1f` |
| `gyeol-cluster-cells-now.json` | `find-gyeol-cluster.mjs` output capturing the 9 cells observed at audit time |
| `run_gates.sh` | Reproduction script for the 18 gates with identity tags |

No `data/`, `src/`, `tools/`, `package.json`, `../namespring/`,
or `../saju-ts/src/` mutation. P26-A5 contributes audit + measurement
only. The `lib/spring-ts/artifacts/phase12-agent-a5/{prose-corpus.json,
prose-corpus.tsv, phase12-audit-issues.json}` files are touched by
`measure_p23.mjs` (corpus refresh + audit-phase12 re-run) — these
files are existing P12-A5 artifacts and the modifications match the
P22-A6 §Rec #5 corpus-freshness contract introduced by P23-A4. They
are reverted (or excluded from commit) to keep this audit's scope
strictly limited to `artifacts/phase26-agent-a5/`.
