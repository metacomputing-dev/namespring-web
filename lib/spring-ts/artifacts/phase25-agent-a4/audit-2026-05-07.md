# Phase 25 Agent A4 — Workflow YAML ship attempt #8 (carry-over)

Date: 2026-05-07
Owner: P25-A4
Branch: `phase25-agent-a4`
Base: `main` HEAD `1213c71f` (`docs(plan): Phase 25 quality
deepening + carry-over closure`)
Scope: 8th consecutive attempt to ship
`.github/workflows/spring-ts-samples-stale.yml`. Owned files:

- `.github/workflows/spring-ts-samples-stale.yml` (drafted then
  removed after OAuth reject)
- `lib/spring-ts/CI_WORKFLOW_INSTRUCTIONS.md` (maintainer-action
  document, ships on this branch)
- `lib/spring-ts/artifacts/phase25-agent-a4/audit-2026-05-07.md`
  (this file)

No mutation to `data/`, `src/`, `tools/`, `package.json`,
`../namespring/`, `../saju-ts/src/`, or any pre-existing workflow
under `.github/workflows/`.

## Headline

**OAuth status: FAIL (8/8 — block is structural, not stochastic).**

`git push -u origin phase25-agent-a4` rejected with the
"refusing to allow an OAuth App to create or update workflow"
signature when the push contained the new workflow YAML file. The
push **succeeded** when the same branch was rewound and reshipped
without the workflow file. P25-A4 therefore delivers
**instruction-doc only** — the YAML body is preserved verbatim
inside `lib/spring-ts/CI_WORKFLOW_INSTRUCTIONS.md` for the
maintainer to apply under their own credentials.

## Carry-over chain (8 attempts, 0 ships)

| Phase | Agent | OAuth result | Ship outcome |
|-------|-------|--------------|---------------|
| P19 | A5 | FAIL | Carry over |
| P20 | A5 | FAIL | Carry over |
| P21 | A5 | FAIL | Carry over |
| P22 | A5 | FAIL | Carry over |
| P22 | A6 | FAIL | Carry over |
| P23 | A5 | FAIL | Carry over |
| P24 | A5 | FAIL | Carry over |
| **P25** | **A4** | **FAIL (this attempt)** | **Instruction doc** |

## Workflow

1. `git fetch origin main && git checkout main && git pull` —
   confirmed `main` HEAD = `1213c71f` (sync).
2. `git checkout -b phase25-agent-a4` (branch identity matches task).
3. Drafted `.github/workflows/spring-ts-samples-stale.yml` per task
   spec. Reviewed alongside existing
   `.github/workflows/deploy-pages.yml` (only pre-existing workflow
   in repo); no modification of the deploy workflow.
4. Drafted `lib/spring-ts/CI_WORKFLOW_INSTRUCTIONS.md` containing:
   - YAML body verbatim (so maintainer can copy-paste);
   - 8-phase carry-over chain;
   - exact maintainer commands to ship under a `workflow`-scoped
     personal access token;
   - non-blocking note flagging that the YAML installs spring-ts
     before building saju-ts (deploy workflow does it the other
     way; safer-ship variant included).
5. Drafted this audit.
6. Stage all 3 files; commit; `git push -u origin phase25-agent-a4`
   with output captured.
7. On OAuth reject (expected per 7/7 prior attempts): `git reset
   --soft HEAD~1`, `git rm .github/workflows/spring-ts-samples-stale.yml`,
   recommit instructions + audit only, push again.
8. Validation: `npm run typecheck` + `npm run test:namespring-compat`
   from `lib/spring-ts`.

## Push attempt log

### Attempt with workflow file

```
$ git add .github/workflows/spring-ts-samples-stale.yml \
          lib/spring-ts/CI_WORKFLOW_INSTRUCTIONS.md \
          lib/spring-ts/artifacts/phase25-agent-a4/audit-2026-05-07.md
$ git commit -m "ci(workflow): ship spring-ts samples-stale PR gate (P25-A4)"
$ git push -u origin phase25-agent-a4
```

Result: rejected with OAuth scope error (signature recorded in
`lib/spring-ts/CI_WORKFLOW_INSTRUCTIONS.md` §"Why this is a
maintainer-owned action"). Sub-agent token lacks the `workflow`
scope GitHub requires to create/modify files under
`.github/workflows/`. Same failure mode reproduced 7 times across
P19–P24.

### Recovery and reship without workflow file

```
$ git reset --soft HEAD~1
$ git rm .github/workflows/spring-ts-samples-stale.yml
$ rm .github/workflows/spring-ts-samples-stale.yml
$ git commit -m "docs(spring-ts): ship workflow ship instructions + P25-A4 audit"
$ git push -u origin phase25-agent-a4
```

Result: success. Branch `phase25-agent-a4` now contains only the
maintainer-action document and this audit. Workflow YAML body is
preserved inside the instruction document for the maintainer to
copy into the actual file under their own credentials.

## Validation gates

After the recovery push, with the workflow file removed:

```
$ npm --prefix lib/spring-ts run typecheck
PASS — tsc --noEmit, 0 errors

$ npm --prefix lib/spring-ts run test:namespring-compat
PASS — 202 / 202
```

(Both gates were unchanged by P25-A4 because no `src/`, `data/`,
`tools/`, or `package.json` content was modified.)

## Acceptance criteria

| Criterion | Status |
|---|---|
| `lib/spring-ts/CI_WORKFLOW_INSTRUCTIONS.md` shipped with verbatim YAML body | MET |
| 8-phase carry-over chain documented | MET |
| Maintainer commands enumerated end-to-end | MET |
| `test:namespring-compat` 202/202 hold | MET |
| `typecheck` pass | MET |
| `.github/workflows/spring-ts-samples-stale.yml` shipped to `main` | NOT MET (OAuth scope, requires maintainer) |
| `data/`, `src/`, `tools/`, `../namespring/`, `../saju-ts/src/` untouched | MET |
| Existing `.github/workflows/deploy-pages.yml` untouched | MET |

## Recommendation for Phase 26+

The 8/8 failure rate confirms this carry-over is **not draining
inside the sub-agent harness**. Two viable paths to close it:

1. **Maintainer-owned ship pinned to one commit per phase**
   (P24-A5 §Rec #2 phrasing). The maintainer applies
   `lib/spring-ts/CI_WORKFLOW_INSTRUCTIONS.md` once; the gate
   becomes part of the standard PR check list and the carry-over is
   formally closed in the next phase audit.

2. **Elevate sub-agent OAuth scope to include `workflow`**
   (out of scope for this agent — requires harness-level config).

Either path closes the recommendation; no further sub-agent ship
attempts will succeed without one of them.

## Files produced this phase

| File | Status on `phase25-agent-a4` |
|------|-------------------------------|
| `lib/spring-ts/CI_WORKFLOW_INSTRUCTIONS.md` | Shipped |
| `lib/spring-ts/artifacts/phase25-agent-a4/audit-2026-05-07.md` | Shipped (this file) |
| `.github/workflows/spring-ts-samples-stale.yml` | **Drafted + push-rejected + reverted** — body preserved inside the instruction doc above |
