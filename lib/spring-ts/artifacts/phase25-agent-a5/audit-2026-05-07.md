# Phase 25 Agent A5 — Comprehensive audit + acceptance measurement

Date: 2026-05-07
Owner: P25-A5
Branch: `phase25-agent-a5`
Base HEAD at audit time: `1213c71f` (`docs(plan): Phase 25 quality deepening + carry-over closure`)
Scope: Phase 25 comprehensive audit. P25-A5 is measurement-only — no
mutation to `data/`, `src/`, `tools/`, `package.json`, `../namespring/`,
`../saju-ts/src/`. Owned scope: `artifacts/phase25-agent-a5/` only.

## Headline

**17 / 18 production CI gates PASS at HEAD `1213c71f`, 1 / 18 FAIL.**

The single failing gate is `ci:samples-stale`, surfacing **4 violations**
(fixtures 05 + 17 + 33 + `index.json`) that trace back to a documented
build-environment / `dist/` non-determinism issue P24-A4 already noted
informationally (their `audit-2026-05-07.md` §"Build-environment note").
Net consequence: the `gyeolCluster.totalCells = 9` lock P24-A3 claimed
was never actually realized in committed artifacts — the committed
samples still embed the pre-paraphrase 결정-cluster prose.

`test:namespring-compat` continues at 202 / 0 PASS. Both new Phase 24
ratchet gates (`ci:standard-paragraph-floor`, `ci:brief-tier-placeholder`)
PASS (sub-3 = 0, PLACEHOLDER = 0). Verb-form 연결, vulnerable
연결+GYEOL_SUBS, post-processor leak, doubled `결이`, hook concentration
max=15, depth_inversion=0, audience_leak=0 all hold at the Phase 23
post-fix baseline.

| Lock | Value at `1213c71f` | Target | Status |
|------|--------------------:|--------|--------|
| Sub-3 standard cells | 0 | 0 | HOLD (P24 ratchet) |
| Brief tier PLACEHOLDER cells | 0 | 0 | HOLD (P24 ratchet) |
| `depth_inversion` hits | 0 | 0 (or ≤ 2) | HOLD |
| Verb-form 연결 grand total | 0 | 0 | HOLD (P21 lock) |
| Vulnerable `_coverage/` 연결+GYEOL_SUBS | 0 | 0 | HOLD (P20 lock) |
| Doubled `결이` cluster | 0 | 0 | HOLD (P20 lock) |
| Post-processor leak occurrences | 0 | 0 | HOLD (P15/P18/P20 lock) |
| `흐름이` cluster cells | 0 | 0 | HOLD (P15 lock) |
| Hook concentration max / median / min | 15 / 10 / 0 | hold (≤ 20) | HOLD (P21 lock) |
| `ci:samples-stale` violations | **4** | 0 | **REGRESS** (committed samples drifted) |
| `ci:hook-coverage` dormant fragmentIds | 0 | 0 | HOLD (P19 lock) |
| `audit-phase12.mjs` audience_leak | 0 | 0 | HOLD (P24-A4 close) |
| `test:namespring-compat` | 202 / 0 | 202 / 0 | HOLD |
| `gyeolCluster.totalCells` | **12** | (P24 reported 9 — see §B) | **DRIFT vs claimed lock** |

Concurrent Phase 25 sibling agents (A1/A2/A3/A4) are **not visible** at
HEAD `1213c71f` — only the `docs(plan)` commit has merged. Sibling
metric impact is therefore not reflected in this audit; their merges
will need a follow-up snapshot.

| Agent | Task | Status visible at HEAD `1213c71f` |
|-------|------|------------------------------------|
| P25-A1 | gyeolCluster 9 hold cells review | Not yet merged |
| P25-A2 | standard 4-band expansion (10-15 fragments) | Not yet merged |
| P25-A3 | hook fragility tail gating review (5 fragments at emit ≤ 4) | Not yet merged |
| P25-A4 | workflow YAML ship attempt (8th carry-over) | Not yet merged |

## Workflow

P25-A5 ran in an isolated worktree at `C:/Projects/metaintelligence/p25-a5-iso`
(P24-A5 pattern) to avoid cross-agent contention with P25-A1 / P25-A2 /
P25-A3 / P25-A4 mutating `data/` and `artifacts/sample-outputs-...` in
the shared main checkout.

1. `git worktree add -b phase25-agent-a5 <iso-path> 1213c71f`.
2. `npm install` in `lib/saju-ts`, `lib/spring-ts`, `lib/seed-ts`
   (fresh worktree had no `node_modules`).
3. `npm --prefix ../saju-ts run build` — clean
   `tsc -p tsconfig.build.json` (saju-math-engine@0.34.3 OK).
4. `node artifacts/phase23-agent-a4/measure_p23.mjs
   artifacts/phase25-agent-a5/post-fix-snapshot-2026-05-07.json` — uses
   the same mtime-aware corpus refresh script P23-A4 authored. Refresh
   fired with `reason=samples-newer-than-corpus` (consistent with
   P23-A5 / P24-A5).
5. 18 production CI gates (`typecheck` + 16 `ci:*` + `test:namespring-compat`)
   invoked with per-gate identity tag
   `[branch=phase25-agent-a5 head=1213c71f]` captured to
   `phase25-acceptance-gates.txt`.

## Pre-fix vs post-fix metric table

Pre-fix column = P24-A5 post-fix at `0290ea65`
(`artifacts/phase24-agent-a5/post-fix-snapshot-2026-05-07.json`),
which is the carry-in baseline for Phase 25.
Post-fix column = this audit at `1213c71f`
(`artifacts/phase25-agent-a5/post-fix-snapshot-2026-05-07.json`).
Phase 25 has no merged data / source changes at audit HEAD beyond the
P24 sibling closures and the `docs(plan)` commit, but P24-A4's data
fix landed between P24-A5's audit and this one — visible as
`audience_leak: 1 → 0`.

| # | Metric | Pre-fix (P24-A5 @ `0290ea65`) | Target | Post-fix (P25-A5 @ `1213c71f`) | Status |
|---|--------|-------------------------------|--------|---------------------------------|--------|
| 1 | Sub-3-paragraph standard cells | 0 | 0 (hold) | 0 | HOLD |
| 2 | Brief tier PLACEHOLDER cells | 0 | 0 (hold) | 0 | HOLD |
| 3 | `depth_inversion` hits (Levenshtein ≥ 0.75) | 0 | 0 (or ≤ 2) | 0 | HOLD |
| 4 | Verb-form 연결 grand total | 0 | 0 (hold) | 0 | HOLD |
| 5 | Vulnerable `_coverage/` 연결+GYEOL_SUBS | 0 | 0 (hold) | 0 | HOLD |
| 6 | Doubled `결이` cluster | 0 | 0 (hold) | 0 | HOLD |
| 7 | Post-processor leak occurrences | 0 | 0 (hold) | 0 | HOLD |
| 8 | `흐름이` cluster cells (≥ 3 in paragraph) | 0 | 0 (hold) | 0 | HOLD |
| 9 | Hook concentration max | 15 | hold (≤ 20) | 15 | HOLD |
| 10 | Hook concentration median | 10 | hold | 10 | HOLD |
| 11 | Hook concentration min | 0 | hold | 0 | HOLD |
| 12 | Sample drift (`ci:samples-stale`) | 0 | 0 (hold) | **4** | **REGRESS** (see §B) |
| 13 | `ci:post-processor-grammar` `--max-violations` | 0 | 0 (hold) | 0 | HOLD |
| 14 | `ci:hook-concentration` `--max-hooks=20` | PASS | PASS (hold) | PASS | HOLD |
| 15 | `ci:hook-coverage` dormant fragmentIds | 0 | 0 (hold) | 0 | HOLD |
| 16 | `test:namespring-compat` | 202/202 PASS | 202/202 | 202/202 | HOLD |
| 17 | Standard tier paragraph histogram (≥ 3) | 1705 (100 %) | 1705 (100 %) | 1705 (100 %) | HOLD |
| 18 | `gyeolCluster.totalCells` | 12 | 9 (per P24-A3 close claim) | 12 | DRIFT vs claimed lock |
| 19 | `audience_leak` (audit-phase12) | 1 | 0 (P24-A4 close) | 0 | CLOSED (P24-A4) |
| 20 | Distinct hook strings emitted | 34 | hold | 34 | HOLD |
| 21 | Authored hook strings | 34 | hold | 34 | HOLD |
| 22 | Lowest emit-count fragility tail (gate's printed 5) | `wealth.thisYear.brief.balanced.neutral.004` (=2); 4 at =4 | hold | same fragment (=2); 4 at =4 | UNCHANGED |
| 23 | Full fragility tail at emit ≤ 4 (custom audit count) | (5) | n/a — diagnostic | **9** (1 dormant + 1 at emit=2 + 7 at emit=4) | EXPANDED |
| 24 | `ci:acceptance-completeness` ciScripts / accepted / exempt | 15 / 14 / 1 | grow with new ratchets | 17 / 16 / 1 | NEW RATCHETS REGISTERED |

### Standard paragraph histogram (no delta)

| paragraphCount | Pre-fix cells (P24-A5) | Post-fix cells (P25-A5) | Delta |
|---|---|---|---|
| 1 | 0 | 0 | 0 |
| 2 | 0 | 0 | 0 |
| 3 | 1385 (81.23 %) | 1385 (81.23 %) | 0 |
| 4 | 83 (4.87 %) | 83 (4.87 %) | 0 |
| 5 | 237 (13.90 %) | 237 (13.90 %) | 0 |
| **Total** | **1705** | **1705** | 0 |

P25-A2's planned 4-band expansion target (10-15 fragments shifted from
3p to 4p) is **not yet merged** at `1213c71f`. The histogram above is
unchanged from P24-A5.

## Phase 25 acceptance gate results

### 18 production CI gates run at HEAD `1213c71f`

P24-A1 (`ci:standard-paragraph-floor`) and P24-A2
(`ci:brief-tier-placeholder`) ratchets are **merged** at this HEAD,
producing a gate count of 18 (vs P24-A5's 16). Both new ratchets PASS
at this audit.

| Gate | Status | Note |
|------|--------|------|
| `typecheck` | PASS | tsc --noEmit, no errors |
| `ci:no-ai-policy` | PASS | fixtureFiles=429, sourceTierRecords=7137 |
| `ci:narrative-voice` | PASS | bundles=345 fragments=4119 (2956/1163), 0/0/0 violations |
| `ci:narrative-truncated-endings` | PASS | filesScanned=344, 0 violations |
| `ci:narrative-tag-label-alignment` | PASS | glossaryTags=208 usedTags=110, 0 missing/mismatch |
| `ci:narrative-orphan-tags` | PASS | filesScanned=344, fragmentsScanned=3954, 0 violations |
| `ci:narrative-cell-axis` | PASS | 165 cells, 0 missing |
| `ci:narrative-density` | PASS | 8 pairs covered, 0 thin/deficit |
| `ci:narrative-tuple-density` | PASS | 3 tuples covered, 0 missing/thin/deficit |
| `ci:narrative-daymaster-tuple-density` | PASS | 1 tuple covered, 0 missing/thin/deficit |
| `ci:post-processor-grammar` | PASS | filesScanned=32, violations=0 (max=0 lock) |
| `ci:hook-coverage` | PASS | 0 dormant by slot; 0 dormant by hook text |
| `ci:hook-concentration` | PASS | max=15 vs threshold=20 (5-hook headroom) |
| `ci:standard-paragraph-floor` | **PASS** (P24-A1 ratchet) | min=3 violations=0 |
| `ci:brief-tier-placeholder` | **PASS** (P24-A2 ratchet) | violations=0, fixturesSkipped=4 (no tieredMatrix) |
| `ci:acceptance-completeness` | PASS | violations=0; ciScripts=17, accepted=16, exempt=1 |
| `ci:samples-stale` | **FAIL** | sampleFiles=37, **violations=4** (see §B) |
| `test:namespring-compat` | PASS | 202 / 0 |

17 / 18 gates PASS, 1 / 18 FAILS (`ci:samples-stale`). Per-gate output
captured to `phase25-acceptance-gates.txt`.

## §B Sample drift root-cause walkthrough (`ci:samples-stale` FAIL)

### B1. Reproduction

`ci:samples-stale` was run twice from this fresh worktree — both runs
produced identical 4-violation output:

```
Samples stale: FAIL
  sampleFiles=37
  violations=4
  FAIL artifacts/sample-outputs-2026-05-05-phase3/05-park-minji-late-night-female-tiered.json
    firstDivergenceOffset=131248
  FAIL artifacts/sample-outputs-2026-05-05-phase3/17-kim-senior-female-tiered.json
    firstDivergenceOffset=78166
  FAIL artifacts/sample-outputs-2026-05-05-phase3/33-consensus-aware-multi-axis-combined-tiered.json
    firstDivergenceOffset=133878
  FAIL artifacts/sample-outputs-2026-05-05-phase3/index.json
    firstDivergenceOffset=10125
```

The drift is reproducible — not transient.

### B2. Same fixture set as the surprising +3 gyeolCluster cells

Diffing this audit's gyeolCluster cell list against P24-A3's
`gyeol-cluster-cells-after.json` ("9 cells after fix") shows exactly
**3 new cells** appearing in this audit's snapshot:

```
  '05-park-minji-late-night-female-tiered|today|wealth|standard|3'
  '17-kim-senior-female-tiered|life|family|standard|0'
  '33-consensus-aware-multi-axis-combined-tiered|today|wealth|standard|3'
```

These are precisely the same fixtures (05, 17, 33) that `ci:samples-stale`
flags. The cluster delta and the samples-stale drift are the same
event, surfaced through two independent measurement paths.

### B3. Spot-check on fixture 05 (`wealth.today` paragraph 3)

The `wealth.today.standard.wildcard.001` fragment in `data/narrative/wealth/today/standard.fragments.json`
has the **post-P24-A3** template text:

```
주의할 결은 즉흥적 결정이에요. 권유받은 큰 거래는 하루 자고 정해도 늦지 않아요.
```

(1 × `결정`, 1 × `결` — does NOT cluster.)

The **committed sample** in `artifacts/sample-outputs-2026-05-05-phase3/05-park-minji-late-night-female-tiered.json`
still contains the **pre-P24-A3** rendered prose:

```
주의할 점은 즉흥적 결정이에요. 권유받은 큰 결정은 하루 자고 결정해도 늦지 않아요.
```

(3 × `결정` — exactly the cluster P24-A3's audit table called out as
"Cell 2 — PARAPHRASE — `결정` × 3".)

So the data fix was committed (`c75bb9c1`) but the rendered-output
artifact was never regenerated against the post-fix data on the
shared-worktree dist that produced the merged samples.

### B4. P24-A4 already noted the underlying cause

P24-A4's audit doc `lib/spring-ts/artifacts/phase24-agent-a4/audit-2026-05-07.md`
§"Build-environment note (advisory, not blocking)" (line 149-161)
explicitly observed:

> A side experiment in P24-A4 attempted `samples:regen` from a freshly
> created sibling worktree (`p24-a4-iso`, `npm install` then build) and
> produced significantly different sample content vs the committed
> samples on main HEAD ... Re-running `samples:regen` from the main
> shared worktree (where the validated `dist/` state lives) produced
> the expected ~137-line diff. This suggests `dist/` build artifacts in
> the main worktree differ from what a clean rebuild produces — worth
> a future Phase audit if precise deterministic-build replay becomes a
> P24+ acceptance criterion.

P25-A5's iso worktree is precisely the "freshly created sibling
worktree, npm install then build" case — and `ci:samples-stale`
correctly flags the drift between the **clean-rebuild dist output** and
the **committed samples produced from the main shared worktree's stale
dist**.

### B5. Net implication

The `gyeolCluster = 9` lock P24-A3 closed exists in the **data layer**
but not in the **rendered-sample artifact layer**. The Phase 25 sibling
P25-A1 (gyeolCluster 9 hold cells review) is therefore building on a
baseline (9 cells) that does not match the actual committed state
(12 cells per measure_p23 + samples-stale drift).

P24-A3 already absorbed this trade-off in their hold list — the 9 cells
they marked HOLD are the exact 9 cells my audit also lists as HOLD
candidates (5 wealth.thisMonth + 4 career.today). The +3 delta cells
(2 × wealth.today wildcard + 1 × family.life elder.008) are exactly
the cells P24-A3 paraphrased in `data/` but whose rendered output never
made it into committed samples.

## Hold-target metrics (regression-free across Phase 25 audit)

The following Phase 18-24 acceptance gates remain at their P24-A5
baseline:

- Verb-form 연결 grand total: 0 (P21-A1 lock)
- Vulnerable `_coverage/` 연결+GYEOL_SUBS: 0 (P20-A2 lock)
- Doubled `결이` cluster: 0 (P20-A1 lock)
- Post-processor leak occurrences: 0 (P15-A2 / P18-A5 / P20-A3 lock)
- `흐름이` cluster cells: 0 (P15-A4 lock)
- Hook concentration max=15 / median=10 / min=0 (P21-A3 lock)
- `ci:post-processor-grammar` `--max-violations`: 0 (P20-A3 lock)
- `ci:hook-concentration` `--max-hooks=20`: PASS
- `ci:hook-coverage` dormant fragmentIds: 0 (P19-A4 lock)
- `ci:standard-paragraph-floor` `--min-paragraphs=3`: 0 violations (P24-A1)
- `ci:brief-tier-placeholder` `--max-cells=0`: 0 violations (P24-A2)
- `audit-phase12.mjs` audience_leak: 0 (P24-A4 close)
- `test:namespring-compat`: 202/202 PASS

## Phase 24 → Phase 25 carry-over residues

These were flagged in P24-A5 §Recommendations and remain at the same
or expanded value here. Listed for tracking continuity.

1. **`gyeolCluster.totalCells = 12` (was claimed 9 in P24-A3)**.
   Audit at `1213c71f` measures 12, not 9. Three cells are surfaced
   that P24-A3's data fix should have eliminated but the rendered
   samples never reflected the data change. Either (a) the samples
   need re-regen on a clean dist, or (b) the lock claim should be
   revised to 12 with the cell list documented.

2. **Authored hook fragility tail expanded** (P24-A5 §Rec #4 carry-over):
   - 1 dormant (emit count 0): `career.thisWeek.brief.age40plus.009`
     `쌓아 온 결이 자리를 받쳐 줘요.` (already counted as
     `dormantHookStringsByText: 1` in snapshot)
   - 1 at emit count 2: `wealth.thisYear.brief.balanced.neutral.004`
     (UNCHANGED from P24-A5)
   - 7 at emit count 4 (P24-A5 listed 4 — net +3 since then):
     - `family.today.brief.young.003`
     - `family.thisMonth.brief.middle.007`
     - `family.thisYear.brief.young.003`
     - `family.life.brief.middle.007`
     - `health.today.brief.strong.elder.001`
     - `romance.thisYear.brief.midlife.001`
     - `study_document.today.brief.adult.005`

   Note: the gate (`ci:hook-coverage`) only prints the lowest-5 tail,
   matching P24-A5's "5 fragments at emit ≤ 4" account. A full
   ≤ 4 sweep finds 9. Both views are accurate; they answer different
   questions. Owned by P25-A3 in this phase.

3. **Workflow YAML ship**:
   `.github/workflows/spring-ts-samples-stale.yml` (eighth consecutive
   carry-over P19/P20/P21/P22-A5/A6/P23-A5/P24-A5/P25-A5 §Recommendations).
   Owned by P25-A4 in this phase. OAuth permission elevation required.

## Corpus freshness verification (P22-A6 §Rec #5 → P23-A4 closed)

`measure_p23.mjs` reports:

```json
{
  "corpusExisted": true,
  "corpusMtimeBefore": 1778145667897,
  "maxSampleMtime":   1778145668646,
  "maxSampleMtimeFile": "diff-legacy-vs-tiered.json",
  "extractScriptMtime": 1778145667872,
  "refreshed": true,
  "reason": "samples-newer-than-corpus",
  "refreshError": null,
  "corpusMtimeAfter": 1778145707497
}
```

P23-A4's mtime-aware corpus refresh continues to fire automatically.
`depth_inversion` reads the post-regen corpus, eliminating the false
18-hit reading P22-A6 hit on first run. This is a hold from P23-A5 /
P24-A5; P25 does not re-investigate.

## Phase 25 acceptance summary

| Acceptance criterion | Status |
|---|---|
| Sub-3 standard cells = 0 (P24 ratchet) | MET (= 0) |
| Brief PLACEHOLDER = 0 (P24 ratchet) | MET (= 0) |
| `depth_inversion` ≤ 2 (hold) | MET (= 0) |
| `audience_leak = 0` (P24-A4 close) | MET (= 0) |
| Corpus freshness automated (P22-A6 §Rec #5) | MET (P23-A4 fix) |
| All 18 production CI gates PASS | **NOT MET — 17/18 (`ci:samples-stale` FAILS)** |
| `test:namespring-compat` 202/202 | MET |
| Hold targets (verb-form / cluster / leak / hook concentration) | MET |
| `gyeolCluster = 9` lock (P24-A3 close claim) | **NOT MET — committed samples show 12** |

**Phase 25 audit verdict**: The Phase 23 / 24 invariants hold at the
data layer, but a build-environment / rendered-artifact gap surfaces
at audit time. The `ci:samples-stale` gate is doing its designed job —
catching exactly the kind of drift that would silently invalidate
downstream measurements (P24-A3's gyeolCluster lock).

## Recommendations for Phase 25 close + Phase 26+

### High priority

1. **P25 close — `ci:samples-stale` FAIL closure**.
   Reproducible 4-violation drift on fixtures 05 / 17 / 33 / `index.json`.
   Closure path: regenerate samples from a build environment that
   produces the same rendered output as the main worktree's dist
   (P24-A4 noted these diverge), then commit the regen. This will
   simultaneously close (a) the samples-stale gate and (b) the
   gyeolCluster lock claim divergence (12 → expected 9). One commit,
   one PR. Recommended as the first Phase 25 sibling close after
   P25-A1's gyeolCluster paraphrase work merges (which expects to read
   the post-P24-A3 cluster baseline).

2. **P26 — Build-determinism investigation**.
   P24-A4 §"Build-environment note" first surfaced the dist-divergence
   between the main shared worktree and a freshly-built sibling
   worktree. P25-A5 confirms the divergence is reproducible and has a
   visible downstream cost (samples-stale FAIL + gyeolCluster lock
   drift). A Phase 26 `dist/` deterministic-replay audit would (a)
   identify what state in the main worktree's dist diverges (TS
   incremental cache? on-disk artifact ordering?), (b) document the
   "canonical regen procedure" that all agents should follow, and (c)
   optionally add a `ci:dist-determinism` gate that compares two
   sequential clean rebuilds.

### Medium priority

3. **P26 — Workflow YAML ship**:
   `.github/workflows/spring-ts-samples-stale.yml`. **Eighth
   consecutive carry-over** (P19/P20/P21/P22-A5/A6/P23-A5/P24-A5/
   P25-A5 §Recommendations). The recommendation has not advanced
   because workflow YAML changes require OAuth permission elevation
   that no single agent has. P25-A4 attempts the ship in this phase;
   if their PR doesn't land, this becomes the ninth carry-over for
   P26. Suggest a maintainer-owned PR pinned to one commit per phase
   to drain this carry-over.

### Low priority (continued from P24-A5 §Rec)

4. **P26 — Authored hook fragility tail expansion**.
   1 dormant + 1 at emit=2 + 7 at emit=4 = 9 fragments at emit ≤ 4.
   The dormant fragment (`career.thisWeek.brief.age40plus.009`) is the
   highest-leverage fix — emit=0 means it never selects under any
   fixture. Authoring 1-2 sibling fragments per low-emit slot would
   lift the tail without changing the gate count or the threshold.
   Carry-over from P21-A5 / P22-A6 / P23-A5 / P24-A5 §Rec #4. Owned
   by P25-A3 in this phase (5-fragment scope per the Phase 25 plan);
   P26 can extend if 9-fragment scope is needed.

5. **P26 — `gyeolCluster.totalCells` settlement**.
   Once Rec #1 (`samples-stale` close) lands, gyeolCluster should drop
   to either 8 (if P24-A3's 9-claim was off by one) or 9 (matching
   P24-A3) or stay at 12 (if regen reveals the shared-worktree dist
   was actually rendering correctly and the iso-worktree dist is the
   one with stale state). Whichever number is reached, formally
   accept it as the steady-state and document the cell list. The
   metric has been stable across P22-A6 / P23-A5 / P24-A5 / P25-A5 at
   12 in committed-sample terms, suggesting that *as committed*, the
   cluster surface is structurally stable.

### Phase 26+ phase-shape options (longer horizon)

6. **Brief tier richness expansion**.
   With sub-3 = 0, PLACEHOLDER = 0, and audience_leak = 0 all locked,
   the brief tier is structurally complete. Phase 26+ could expand
   brief.hook authored count from 34 to ~50 to widen the selection
   pool, address the dormant + emit≤2 fragility tail, and reduce
   concentration max from 15 → ~12 (current threshold 20 has 5-hook
   headroom; even 12 would tighten without breaking).

## Files produced this phase

Owned scope (`artifacts/phase25-agent-a5/`):

| File | Purpose |
|------|---------|
| `audit-2026-05-07.md` | This document |
| `phase25-acceptance-gates.txt` | All 18 production CI gates, each with per-gate identity tag |
| `post-fix-snapshot-2026-05-07.json` | Output of `measure_p23.mjs` against current main HEAD `1213c71f` |
| `gyeol-cluster-cells-now.json` | `find-gyeol-cluster.mjs` output capturing the 12 cells observed at audit time |
| `run_gates.sh` | Reproduction script for the 18 gates with identity tags |

No `data/`, `src/`, `tools/`, `package.json`, `../namespring/`,
or `../saju-ts/src/` mutation. P25-A5 contributes audit + measurement
only. The `lib/spring-ts/artifacts/phase12-agent-a5/{prose-corpus.json,
prose-corpus.tsv, phase12-audit-issues.json}` files are touched by
`measure_p23.mjs` (corpus refresh + audit-phase12 re-run) — these
files are existing P12-A5 artifacts and the modifications match the
P22-A6 §Rec #5 corpus-freshness contract introduced by P23-A4. They
are reverted (or excluded from commit) to keep this audit's scope
strictly limited to `artifacts/phase25-agent-a5/`.
