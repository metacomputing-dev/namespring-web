# P20-A3 Audit — Sample Regen + ci:post-processor-grammar Ratchet

Date: 2026-05-07
Branch: p20-a3-samples-regen
Base: origin/main @ 7229fb5 (post P20-A1 + P20-A2 + P20-A4)

## Scope

Owned outputs (per task):
- `lib/spring-ts/artifacts/sample-outputs-2026-05-05-phase3/*.json` (regenerated)
- `lib/spring-ts/package.json` (`ci:post-processor-grammar` `--max-violations` 2 → 0)
- `lib/spring-ts/artifacts/phase20-agent-a3/audit-2026-05-07.md` (this doc)

Out of scope (untouched): `../namespring/`, `src/`, `data/`, `tools/`, API/contract.

## Goal

P19-A2 set `ci:post-processor-grammar --max-violations=2` as a ratchet to capture
2 known `연흐름` GYEOL_SUBS leak instances that survived P19-A3's engine
lookbehind fix because the committed sample artifacts were generated before
P19-A3 landed in the engine binding. With P20-A1 (doubled `결이` data fix),
P20-A2 (`연결입니다` cleanup), and the existing P19-A3 lookbehind all on
origin/main, regenerating samples should reduce the count to 0 and make the
ratchet collapsible.

## Pre-regen baseline

`node tools/check_post_processor_grammar.mjs --json --max-violations=999`
against committed samples on origin/main:

```
totalViolations: 2
ruleCounts: (?<![가-힣])연흐름: 2  (all other rules: 0)

Sample 1:
  file:  25-jonggyeok-jonggwan-tiered.json
  trail: payload.tieredMatrix.periods.life.overall.expert.paragraphs.0.plainText
  text:  "90세 이후는 변화의 크기보다 안정, 존중, 기억의 정리, 편안한 연흐름을 ..."
Sample 2:
  file:  35-palace-naeum-female-mid-tiered.json
  trail: payload.tieredMatrix.periods.today.overall.standard.paragraphs.0.plainText
  text:  "도움이 되는 기운이 나무 기운일 때 오늘은 작은 성장과 연흐름을 ..."
```

Source-data spot-check confirmed the corresponding fragments under
`data/narrative/_coverage/` already carry `연결고리` (correct), e.g.

- `age-phase-expert-mature.fragments.json:986` → `편안한 연결고리를 중심으로 보는 것이 적절합니다`
- `yongshin-element-daily-support.fragments.json:22` → `작은 성장과 연결고리를 의식하면 흐름을 잡기 쉬워요`

So both surface defects were stale-sample artifacts; the source data and the
P19-A3 engine lookbehind are already correct, only the sample regen step had
not yet been run.

Source-side broad sweep:
`Grep '(?<![가-힣])연결[을이로의에과도만]' data/narrative` → 0 matches.

## Regen execution

```
npm --prefix ../saju-ts run build       # OK
npm run samples:regen                   # OK
```

Diff vs HEAD (sample-outputs only):
- 36 files changed, 165 insertions(+), 165 deletions(-)
- 35 `*-tiered.json` plus `index.json`
- Net text length unchanged at the line level (most diffs are localized
  particle/word swaps where the engine's GYEOL_SUBS no longer fires on
  `연결X` and the original Korean compound survives).

## Post-regen measurement

`ci:samples-stale`: PASS, sampleFiles=37, violations=0.

`ci:post-processor-grammar` (with `--max-violations=0`):
```
filesScanned=32, violations=0 (max=0)
all 17 rule patterns: 0
```

Ratchet → 0 is therefore safe. `package.json` updated:
`"ci:post-processor-grammar": "node tools/check_post_processor_grammar.mjs --max-violations=0"`.

## Validation suite

| Gate                                | Result |
| ----------------------------------- | ------ |
| `npm run typecheck`                 | PASS (no output, 0 errors) |
| `npm run ci:no-ai-policy`           | PASS |
| `npm run ci:narrative-voice`        | PASS (plain term/tag/expert-untagged all 0) |
| `npm run ci:samples-stale`          | PASS (37 files, 0 violations) |
| `npm run ci:post-processor-grammar` | PASS (0 violations at max=0) |
| `npm run ci:hook-coverage`          | PASS (34 fragments / 34 hooks / 0 dormant) |
| `npm run ci:acceptance-completeness`| PASS (14 ciScripts, 0 violations) |
| `npm run test:namespring-compat`    | 202 PASS / 0 FAIL |

## Constraints honored

- No edits to `../namespring/`, `src/`, `data/`, or `tools/`.
- API/contract surfaces unchanged (namespring-compat 202/202 confirms).
- `package.json` change is the single `--max-violations=2` → `0` token swap on
  the existing `ci:post-processor-grammar` line; no other script touched.

## Commit plan

Two commits per task spec:
1. `artifacts(phase3-samples): regenerate samples after P20 narrative fixes`
   — 36 sample files
2. `ci(post-processor-grammar): collapse compound 연결X ratchet to 0`
   — package.json one-line + this audit doc
