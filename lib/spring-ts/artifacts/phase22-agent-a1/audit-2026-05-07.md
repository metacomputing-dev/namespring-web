# Phase 22 Agent A1 — Path A lift + brief-tier regression guard

Date: 2026-05-07
Branch: `phase22-agent-a1`
Commits:
- `123c063` data(narrative): P22-A1 wealth.brief minor-band fragments (5 fragments, +241 LOC data)
- `f237bdd` src(tiered): P22-A1 lift MINOR_LIMITED_CATEGORIES early-return (+8/-11 LOC src)
- `70af70c` artifacts(phase3-samples): regenerate samples after P22-A1 (36 sample files)

Owned scope (per Phase 22 plan):
- `lib/spring-ts/src/report/tiered/build-tiered-matrix.ts:47-51, 332-335`
- `lib/spring-ts/data/narrative/wealth/{life,today,thisWeek,thisMonth,thisYear}/brief.fragments.json`
- `lib/spring-ts/artifacts/phase22-agent-a1/audit-2026-05-07.md`

Out of scope:
- `wealth/{period}/standard.fragments.json` (P22-A3 territory)
- `career/health_stress/health/overall` minor-band fragments (P22-A2 territory)

## 1. Pre-measurement (read-only, before lift)

`measure_brief_tier.mjs` mirrors `passesMinorGuard` semantics from
`fragment-selector.ts:53-62` against `data/narrative/**/*.fragments.json`,
checking which (cat × period × ageBand) combinations have a minor-gated
brief fragment for the 5 minor fixtures in the sample suite.

Result before authoring:

| metric | value |
|---|---|
| total cells (3 cats × 5 periods × 5 fixtures) | 75 |
| fragment-found | 54 |
| would regress to PLACEHOLDER_BRIEF after src lift | 21 |

Distribution of the 21 cells (all in `wealth`):

```
wealth | life      | 0-9   | 4 cells
wealth | today     | 0-9   | 4 cells
wealth | today     | 10-19 | 1 cell
wealth | thisWeek  | 0-9   | 4 cells
wealth | thisMonth | 0-9   | 4 cells
wealth | thisYear  | 0-9   | 4 cells
```

`romance` and `study_document` brief tiers were already covered for
both 0-9 and 10-19 across all 5 periods (existing `child.001`,
`teen.001`, `young.004`, `ageband.child_teen.501`, `ageband.student.501`,
etc. fragments).

## 2. Brief fragment authoring (commit 123c063)

5 new fragments authored, mirroring the existing minor-brief schema
(single text token + slot, no hook, no tags, T1_HYPOTHESIS sourceTier):

| fragmentId | gating.ageBand | period |
|---|---|---|
| `wealth.life.brief.child.0_9.013` | `['0-9']` | life |
| `wealth.today.brief.child_teen.012` | `['0-9','10-19']` | today |
| `wealth.thisWeek.brief.child.0_9.012` | `['0-9']` | thisWeek |
| `wealth.thisMonth.brief.child.0_9.011` | `['0-9']` | thisMonth |
| `wealth.thisYear.brief.child.0_9.013` | `['0-9']` | thisYear |

All headlines verified ≤28 자모 after rendering (brief contract limit
per `template-engine.ts:858 compressBriefHeadlineIfApplicable`).
Voice: 용돈/저금통/아끼는 마음 — age-appropriate, jargon-free,
matches existing `wealth.{thisMonth,thisYear,life}.brief.10_19.0X`
adolescent-band style.

After authoring, `measure_brief_tier.mjs` re-run:

| metric | value |
|---|---|
| total cells | 75 |
| fragment-found | 75 |
| would regress to PLACEHOLDER_BRIEF | **0** |

## 3. Src lift (commit f237bdd)

Removed `MINOR_LIMITED_CATEGORIES` const (lines 47-51) and the
guarded early-return in `buildCell` (lines 332-335). Replaced with a
3-line comment explaining the post-selection fallback semantics
remain.

Diff:
- 1 file changed, 8 insertions(+), 11 deletions(-)
- Net src reduction: 9 LOC (5-line const + 4-line guard, minus 3-line
  explanatory comment)

## 4. Sample regen + sub-3 measurement (commit 70af70c)

`npm run samples:regen` regenerated all 37 sample files in
`artifacts/sample-outputs-2026-05-05-phase3/`.

Standard-paragraph histogram across all 35 tiered fixtures × 5
periods × 11 cells = 1705 cells total:

| paragraphs | pre-P22-A1 (P18 baseline) | post-P22-A1 |
|---|---|---|
| 1 | 107 | 55 |
| 2 | 84 | 21 |
| 3+ | 1514 | 1629 |

Sub-3 with no fragmentId (P21-A4 metric) — 1-paragraph fallback cells:

| metric | pre-lift | post-lift | target |
|---|---|---|---|
| no-fid 1-paragraph cells | 107 | **55** | ≤55 |

Distribution of the 55 no-fid sub-3 cells (matches P21-A4 §2/§3
prediction exactly):

```
By category:
   23  wealth      (P22-A3 owns standard authoring)
   16  career      (P22-A2 owns)
    8  health_stress (P22-A2 owns)
    4  health      (P22-A2 owns)
    4  overall     (P22-A2 owns)
```

Brief-tier regression check (275 minor cells across 5 fixtures × 5
periods × 11 categories):

| metric | value |
|---|---|
| PLACEHOLDER_BRIEF (`준비 중인 흐름이에요.`) headlines | **0** |

Spot-check on `18-lee-child-male-tiered.json` (0-9 fixture):

```
wealth.life.brief.headline    : '평생의 재물운은 아끼는 마음이 자라는 시기예요.'
wealth.today.brief.headline   : '오늘은 갖고 싶은 것을 잠깐 미루면 좋은 하루예요.'
wealth.thisWeek.brief.headline: '이번 주는 용돈 메모를 적어 보는 한 주예요.'
wealth.thisMonth.brief.headline: '이번 달은 저금통 모으기가 즐거운 한 달이에요.'
wealth.thisYear.brief.headline: '올해는 아끼는 즐거움이 자라는 한 해예요.'
romance.life.standard.paragraphs: 2 (fragmentId=romance.life.standard.ageband.child_teen.501)
```

The wealth.life.brief headline shows `재물운은` instead of authored
`재물 흐름은` — that is the `compressBriefHeadlineIfApplicable`
post-pass at `template-engine.ts:858` collapsing
`재물 흐름은 → 재물운은` to fit the 28 자모 invariant. Behaviour is
correct and idempotent.

## 5. Validation

```
npm run typecheck                       -> PASS (silent)
npm run ci:no-ai-policy                 -> PASS (4082 fragments, 7100 records, 54 src files)
npm run ci:narrative-voice              -> PASS (0 plain term, 0 plain tag, 0 expert untagged)
npm run ci:narrative-truncated-endings  -> PASS (344 files, 0 violations)
npm run test:narrative-schema:summary   -> PASS (103038 / 0)
npm run test:tiered-shape               -> PASS (916 / 0)
npm run test:tiered-isolation           -> PASS (37 / 0)
npm run test:tiered-determinism         -> PASS (4 / 0)
npm run ci:post-processor-grammar       -> PASS (all gates 0)
npm run ci:hook-coverage                -> PASS (exit 0)
npm run ci:hook-concentration           -> PASS (exit 0)
npm run ci:samples-stale                -> PASS (37 sample files, 0 violations)
npm run test:namespring-compat          -> PASS (202 / 0)
```

## 6. Composability with P22-A2/A3

The 55 residual no-fid sub-3 cells are entirely in P22-A2/A3 scope:

- 23 wealth cells (5 periods × 4-5 fixtures, 0-9 ∪ 10-19 ageBand) →
  P22-A3 will author wealth standard fragments
- 16 career cells (4 periods × 4 fixtures, 0-9) → P22-A2
- 8 health_stress cells (2 periods × 4 fixtures, 0-9) → P22-A2
- 4 health cells (4 periods × 1 fixture, 10-19) → P22-A2
- 4 overall cells (4 periods × 1 fixture, 10-19) → P22-A2

When P22-A2 and P22-A3 land their fragment authors, the sub-3 count
should reach 0 per the P21-A4 audit §4 trajectory.

This audit's regenerated samples reflect P22-A1 alone (Path A only).
The brief-tier regression guard is fragments-on-disk, so P22-A2/A3
can layer their standard fragment work without disturbing the brief
tier state.

## 7. Files

- `audit-2026-05-07.md` — this file
- `measure_brief_tier.mjs` — pre/post measurement script (mirrors
  `passesMinorGuard` semantics)
- `measure_brief_tier.json` — post-authoring measurement output
  (75/75 fragment-found, 0 PLACEHOLDER_BRIEF)
