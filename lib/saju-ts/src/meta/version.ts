// Browser-safe engine metadata.
//
// In this web embedding, we avoid Node-only module loading (`node:module`).
// Keep these values aligned with package.json when versioning.

export const ENGINE_NAME: string = 'saju-math-engine';
export const ENGINE_VERSION: string = '0.34.3';
